# Migration Manifest

Every applied migration must appear here. Verified by `scripts/check-migration-manifest.sh`.

| version | filename | classification | summary | PR |
|---|---|---|---|---|
| 20260407100420 | 20260407100420_populate_country_phone_codes.sql | (historical) | populate_country_phone_codes | |
| 20260409000000 | 20260409000000_baseline_schema.sql | (historical) | baseline_schema | |
| 20260409104128 | fix_schema_mismatches_for_frontend | (historical) | fix_schema_mismatches_for_frontend | |
| 20260409114712 | add_profile_fks_for_postgrest_joins | (historical) | add_profile_fks_for_postgrest_joins | |
| 20260513174236 | add_tenants_theme_column | (historical) | add_tenants_theme_column | |
| 20260514074316 | add_log_case_checkout_function_and_portal_visibility_unique | (historical) | add_log_case_checkout_function_and_portal_visibility_unique | |
| 20260514075237 | portal_auth_password_verification_p0_1 | (historical) | portal_auth_password_verification_p0_1 | |
| 20260514081548 | add_tenant_audit_trigger_to_customer_company_relationships | (historical) | add_tenant_audit_trigger_to_customer_company_relationships | |
| 20260525000840 | add_restrictive_policies_profiles_and_platform_audit_logs | RLS-only | Phase 6 M1 — add RESTRICTIVE tenant-isolation + platform-only policies (defense-in-depth) | |
| 20260525001115 | add_tenant_and_audit_trigger_to_eligible_tables | Additive | Phase 6 M2a — bulk-apply set_*_tenant_and_audit trigger to 100 tenant tables already having required audit columns | |
| 20260525001451 | add_audit_columns_and_trigger_to_remaining_tenant_tables | Conditional rename | Phase 6 M2b — add created_at/updated_at where missing on 55 tenant tables, then apply trigger | |
| 20260525001551 | billing_events_tenant_id_not_null_and_cascade | Conditional | Phase 6 M3 — billing_events.tenant_id SET NOT NULL + FK ON DELETE CASCADE | |
| 20260525024225 | revoke_execute_on_internal_trigger_functions | RLS-only | Phase 6 M6 — REVOKE EXECUTE on set_tenant_and_audit_fields + update_updated_at_column + handle_new_user from anon/authenticated (closes RPC exposure) | |
| 20260525024232 | convert_security_definer_views_to_security_invoker | RLS-only | Phase 6 M7 — security_invoker=true on public.customers + v_chain_of_custody_timeline (prevents cross-tenant leak via views) | |
| 20260525024302 | harden_function_search_paths | RLS-only | Phase 6 M9 — SET search_path on 11 functions (closes schema-poisoning vulnerability) | |
| 20260525030723 | create_supplier_documents_storage_bucket | Additive | CI fix — creates supplier-documents bucket + RLS policies (DocumentUploadModal was targeting non-existent 'documents' bucket) | |
| 20260525053058 | p0_bugfix_sweep_small_additive | Additive | P0 sweep A3+A6+A7+A13 — inventory_search_templates description/usage_count, stock_locations is_default/sort_order, financial_transactions status/ref/notes, payroll_adjustments+database_backups+kb_article_versions missing cols | |
| 20260525053117 | p0_bugfix_sweep_template_doc_salary | Additive | P0 sweep A10+A11+A12 — master_template_categories metadata + master_template_types relations, document_templates 7 cols, salary_components 5 cols | |
| 20260525053135 | p0_bugfix_sweep_stock_items_and_sales | Additive | P0 sweep A4+A5 — stock_items 9 form-field cols, stock_sales payment_status/method_id | |
| 20260525053212 | p0_a1_invoice_conversion_linkage | Additive | P0-A1 — invoices.converted_to_invoice_id + proforma_invoice_id + converted_at + FK on converted_from_quote_id + 3 partial indexes (unlocks CaseFinancesTab chain UI) | |
| 20260525053219 | p0_a2_stock_sale_item_invoice_linkage | Additive | P0-A2 — stock_sale_items.invoice_line_item_id FK + stock_sales.invoice_id FK (re-enables addSaleToInvoice flow deleted in C2 sweep) | |
| 20260525053226 | p0_a8_bank_transactions_debit_credit_split | Additive | P0-A8 — debit_amount + credit_amount generated columns + running_balance + composite index | |
| 20260525053232 | p0_a9_payments_case_id_direct_link | Additive | P0-A9 — payments.case_id FK + backfill from invoices.case_id (restores PaymentsList "View Related Case" path) | |
| 20260525053307 | p0_s4_audit_trail_capture_ip_and_ua | RLS-only | P0-S4 part 1 — log_audit_trail RPC now captures IP + User-Agent from PostgREST request.headers session var; accepts optional override params | |
| 20260525053340 | p0_s4_audit_trails_immutability | RLS-only | P0-S4 part 2 — REVOKE UPDATE/DELETE on 9 audit tables from authenticated/anon + prevent_audit_mutation() trigger as belt-and-suspenders (audit log append-only enforcement) | |
| 20260525053417 | p0_s5_mfa_enforcement_scaffold | Additive | P0-S5 — tenants.require_mfa_for_admins bool DEFAULT false (frontend enforcement deferred to follow-up commit) | |
| 20260525060200 | p0_s7_state_machine_phase_buckets_and_backfill | Additive | P0-S7 part 1 — UPDATE master_case_statuses.type into canonical phase enum (intake/diagnosis/quoting/awaiting_approval/approved/recovery/qa/ready/completed/delivered/cancelled), add CHECK constraint + index, backfill cases.status_id from cases.status text | |
| 20260525060235 | p0_s7_case_status_transitions_table | Additive | P0-S7 part 2 — case_status_transitions table modeling allowed phase edges (20 rows seeded: 18 forward + 2 admin reopens), RLS+force, platform-admin write, authenticated read | |
| 20260525060311 | p0_s7_transition_case_status_rpc | RLS-only | P0-S7 part 3 (initial) — transition_case_status RPC (SECURITY DEFINER): validates phase edge, checks role allowlist, audits to case_job_history. Replaced by v2 below (column-name fix). | |
| 20260525060400 | p0_s7_transition_case_status_rpc_v2_fix_audit_cols | RLS-only | P0-S7 part 3 (final) — RPC corrected to use case_job_history actual columns (old_value/new_value/details — packs phase+reason+notes as compact JSON in details text col) | |
| 20260525062142 | p0_s8_notification_engine_tables | Additive | P0-S8 part 1 — 5 tenant-scoped tables (notification_events/subscriptions/log/templates/webhooks) with full 7-element compliance, dedup UNIQUE on (tenant_id, dedup_key), indexes for unread + entity lookup | |
| 20260525062210 | p0_s8_emit_notification_event_helper | RLS-only | P0-S8 part 2 — emit_notification_event RPC (SECURITY DEFINER, locked-down EXECUTE) writes to notification_events with default dedup-by-hour. Single entry point for all event emission. | |
| 20260525062232 | p0_s8_transition_case_status_emits_event | RLS-only | P0-S8 part 3 — transition_case_status RPC now emits case.phase_changed event after each successful transition with full payload (case_no, from/to status + phase, customer_id, assigned_engineer_id, reason, notes) | |
| 20260525062333 | p0_s8_seed_default_templates_and_subscriptions_v2 | Additive | P0-S8 part 4 — system-default templates for case.phase_changed (in_app + email, EN), bulk-subscribed every existing staff user to case.phase_changed in_app, trg_seed_default_notification_subscriptions auto-subscribes new profiles | |
| 20260525062411 | p0_s8_dispatch_in_app_channel_via_trigger | RLS-only | P0-S8 part 5 — render_notification_template SQL helper + dispatch_notification_event_in_app AFTER INSERT trigger on notification_events. Fans out per matching subscription, renders template, writes notification_log. Email/SMS/webhook channels deferred to follow-up (need edge function for external I/O). | |
| 20260525082709 | p0_s8b_enable_pg_net_and_email_dispatch_trigger | Additive | P0-S8b — CREATE EXTENSION pg_net (async outbound HTTP). system_settings_internal table for platform-admin-managed config (edge_function_base_url, edge_function_service_key). get_system_setting() SECURITY DEFINER helper. dispatch_notification_event_email() AFTER INSERT trigger on notification_events calls notification-dispatch-email edge function via net.http_post with the event_id, only when email subscriptions exist (avoids wasted edge invocations). | |
| 20260525083649 | p0_s8c_customer_visibility_and_portal_subscriptions | Additive | P0-S8c — master_case_statuses.customer_visible boolean (false for diagnosis/recovery/qa internal phases, true for everything else). Customer-friendly templates for case.phase_changed.customer (email + in_app). transition_case_status RPC emits separate customer event only when target status is customer_visible AND case has customer_id. trg_seed_portal_customer_subscriptions auto-subscribes portal_enabled customers. Bulk-seed for 6 existing portal customers (email + in_app). | |
| 20260525143740 | p0_s7b_state_machine_bypass_guard | RLS-only | P0-S7b — guard_cases_status_changes() BEFORE UPDATE trigger on cases blocks direct status/status_id modifications outside transition_case_status RPC. RPC sets session-local app.bypass_status_guard='true' before its UPDATE then clears it. Emergency-repair bypass via SELECT set_config('app.bypass_status_guard', 'true', true) before the manual UPDATE in same transaction. | |
| 20260526051941 | p0_s8d_time_based_event_triggers | Additive | P0-S8d — CREATE EXTENSION pg_cron. process_time_based_events() SECURITY DEFINER scans for expiring quotes (next 48h) + overdue invoices (7d/14d/30d tiers) and INSERTs into notification_events with day-bucketed dedup keys. cron.schedule fires it every 15 min. Seeded templates + role-tiered subscriptions (sales/accounts get expiry + 7d; manager/admin/owner also get 14d + 30d escalations). seed_default_notification_subscriptions extended to auto-subscribe new users per role. | |
| 20260526052337 | p1_payment_received_event_and_inventory_low_stock_bridge | Additive | P1 — emit_payment_received_event() AFTER INSERT trigger on payments fires payment.received (staff) + payment.received.customer (when customer_id present). emit_stock_alert_event() bridges existing stock_alerts INSERTs into the unified notification_events stream as inventory.low_stock / inventory.out_of_stock. Templates seeded for all 4 events (staff + customer-friendly receipt copy). Subscriptions: sales/accounts/manager/admin/owner → payment.received; technician/manager/admin/owner → inventory alerts; all portal customers → payment.received.customer. Seed triggers extended. | |
| 20260526073325 | p1_b1_clone_drives_lifecycle_columns | Additive | P1-B1 — adds 24 columns to clone_drives (storage_path/server/type, image_format/size_gb/expected_size_gb, clone_date + cloned_by + cloned_by_name, delivered_date + delivered_by + delivered_by_name + delivery_notes, extracted_date + extracted_by, archived_date + archived_by, retention_days + retention_deadline, preserve_reason + preserved_by + preserved_date, resource_clone_drive_id + physical_location_id FKs, attributes jsonb). Status CHECK enum tightened to active/delivered/extractable/extracted/preserved/archived/failed/deleted. set_clone_drive_retention_deadline trigger auto-computes retention_deadline. 4 indexes for hot paths (clone_date, retention_deadline, resource_id, case_status). | |
| 20260526073420 | p1_sla_tracking_and_breach_events | Additive | P1 — cases.phase_entered_at column (backfilled from updated_at). tenant_sla_policies table (priority × phase × target_hours, full 7-element compliance). transition_case_status RPC updated to set phase_entered_at on every transition. process_time_based_events extended with SLA-breach pass that joins cases × master_case_statuses × tenant_sla_policies, emits case.sla_breach when phase_entered_at + target_hours < now(). Templates + manager-tier subscriptions seeded. | |
| 20260529194317 | add_currency_decimals_and_exchange_rates | Additive | Phase 1 multi-currency M1 — master_currency_codes.decimal_places (JPY/KRW=0; BHD/JOD/KWD/OMR=3; rest=2). New global exchange_rates table (base/quote/rate/rate_date/source/provider, pivot model, unique+distinct-ccy constraints, lookup index) with RLS: global SELECT for authenticated, write restricted to is_platform_admin() (service_role ingestion bypasses). No existing tenant/financial tables modified. | |
| 20260529195321 | tenant_base_currency_and_supported_currencies | Additive | Phase 1 multi-currency M2 — tenants.base_currency_code (mandatory base/reporting currency; backfilled from currency_code, SET NOT NULL, FK to master_currency_codes). New tenant_currencies table (tenant_id, currency_code FK, is_base, is_active, display_order; standard audit columns) with RESTRICTIVE tenant isolation + permissive ops + admin-only delete RLS, set_tenant_and_audit_fields trigger, tenant partial index + exactly-one-base partial-unique index. Base row backfilled per tenant BEFORE trigger/RLS (avoids the cross-tenant insert guard). | |
| 20260529200106 | document_currency_rate_and_base_amounts | Additive | Phase 1 multi-currency M3 — per-document frozen exchange_rate (numeric(20,10) default 1) + rate_source (CHECK provider/manual/derived) + base-currency amount columns on invoices (subtotal/tax/total/amount_paid/balance_due _base), quotes (subtotal/tax/total _base), payments/financial_transactions (amount_base), expenses (amount/tax _base). Widened all money columns numeric(12,2)->(19,4) (incl. invoice_line_items/quote_items) for 0/2/3-decimal currency precision; tax_rate(5,2) unchanged. *_base backfilled = raw amount (rate 1 today) and left NULLABLE so pre-service-update writes do not fail (reporting COALESCEs base, amount*rate). Financial tables were empty -> zero-row rewrite. | |
| 20260529202552 | base_currency_stats_rpcs_and_realized_fx | Additive | Phase 1 multi-currency M4 — base-currency aggregate RPCs get_invoice_stats_base(p_case_id)/get_quote_stats_base()/get_expense_stats_base() returning jsonb (status/type counts + base-currency sums via COALESCE(base, amount*exchange_rate); replace the fetch-all-then-reduce JS aggregation and fix cross-currency summing). compute_realized_fx(doc_amount, payment_rate, invoice_rate, base_currency) returns the settlement FX delta rounded to the base currency's decimal_places (verified USD 3.00 / OMR 0.500 / JPY 0). All SECURITY INVOKER (RLS scopes to tenant), search_path hardened, EXECUTE granted to authenticated. No DML; no existing object altered. | |
| 20260529203637 | schedule_daily_exchange_rate_sync | Additive | Phase 1 multi-currency M5 — invoke_sync_exchange_rates() SECURITY DEFINER fn does net.http_post to the sync-exchange-rates edge function using get_system_setting(edge_function_base_url/service_key) (mirrors dispatch_notification_event_email pattern); EXECUTE revoked from anon/authenticated. pg_cron job sync-exchange-rates-daily scheduled '30 5 * * *'. Pairs with the deployed sync-exchange-rates edge function (Deno; open.er-api.com primary / openexchangerates via OPENEXCHANGERATES_APP_ID; USD pivot; upserts exchange_rates). Dormant until edge_function_service_key is populated in system_settings_internal (same admin config the existing email dispatch depends on). | |
| 20260530051339 | fix_tenant_base_currency_provisioning | Hotfix (function/trigger) | Phase 1 multi-currency M-fix part 1 — M2 added tenants.base_currency_code NOT NULL but no creation path set it, so EVERY new-tenant INSERT failed (signup broken). sync_tenant_config_from_country() BEFORE INSERT trigger now resolves currency_code/symbol/decimals with COALESCE fallbacks ('USD'/'$'/2) and always backstops NEW.base_currency_code := COALESCE(NEW.base_currency_code, NEW.currency_code, 'USD'). New seed_tenant_base_currency() AFTER INSERT trigger on tenants seeds the base row in tenant_currencies (ON CONFLICT (tenant_id, currency_code) DO NOTHING). No table/column/type changes → database.types.ts unchanged. | |
| 20260530051557 | fix_tenant_base_currency_seed_bypass_guard | Hotfix (function) | Phase 1 multi-currency M-fix part 2 — the M-fix part-1 seed trigger's INSERT into tenant_currencies tripped set_tenant_and_audit_fields()'s cross-tenant guard during provision-tenant (service_role: auth.uid() NULL → get_current_tenant_id() NULL and is_platform_admin() false → "Cannot insert data for a different tenant"), re-breaking signup. Added opt-in transaction-local bypass app.bypass_tenant_guard (same app.bypass_* pattern as p0_s7b state-machine guard): the INSERT cross-tenant check is skipped only when set to 'true'; UPDATE tenant-immutability check, audit timestamps, and default behavior absent the flag are unchanged. seed_tenant_base_currency() sets the flag local before its insert and clears it after. No table/column/type changes → database.types.ts unchanged. | |
| 20260530123447 | fix_convert_proforma_carry_currency_rate_base | Hotfix (function) | Phase 1 multi-currency P0 fix — convert_proforma_to_tax_invoice() dropped the functional-currency snapshot (its invoice INSERT omitted currency/exchange_rate/rate_source/*_base), so every proforma→tax conversion produced a USD / rate-1 / NULL-base invoice — silent base corruption + wrong currency even for single-currency OMR tenants (invoices.currency defaults 'USD', *_base default NULL). RPC now copies the quote's frozen currency, exchange_rate, rate_source and subtotal/tax/total _base verbatim, and sets amount_paid_base=0 + balance_due_base=total_amount_base. Verified RED→GREEN against the live DB (EUR@0.42 proforma → EUR/0.42/44.1-base invoice). Body-only change → database.types.ts unchanged. | #fix-phase1-mc-p0s |
| 20260531163225 | add_device_diagnostics_active_device_unique_index | Additive | Critical-fix C1 (diagnostics persistence) — partial UNIQUE index idx_device_diagnostics_device_unique on device_diagnostics(device_id) WHERE deleted_at IS NULL. Enforces one active diagnostics row per device so the read-then-write upsert cannot create duplicates that make .maybeSingle() (service + report reader) throw PGRST116. Index-only → database.types.ts unchanged. | |
| 20260531224655 | harden_case_write_surface_block_terminal_seed_and_gate_evidence | RLS-only | Step-3 write-surface hardening (prerequisite for the C3/C4 release gates). cases_insert WITH CHECK now forbids birthing a case at a terminal/release status (type IN completed/delivered/ready/cancelled, by name AND status_id) — closes the INSERT-seed bypass where any authenticated REST caller could create a 'Delivered' case and skip checkout/QA/payment (guard_cases_status_changes is UPDATE-only, so it never saw INSERTs). case_qa_checklists + case_recovery_attempts INSERT/UPDATE WITH CHECK tightened true→is_staff_user() so non-staff (viewer/portal) can't forge QA-pass / recovery-result evidence. Verified RED→GREEN via authenticated-role simulation (Delivered seed 42501-blocked; Received still inserts). No table/column/type change → database.types.ts unchanged. Deferred to the C3 gate PR (need the release RPC + capture vocabulary): clone_drives delivered-status trigger, QA/recovery value CHECKs, completed_by=auth.uid()/reviewer≠performer. | |
| 20260601062647 | c3_enforce_release_gate_in_transition_and_route_checkout | Function + additive CHECK | C3 release gate (urgent tier). transition_case_status now ENFORCES the evidence-backed case_status_transitions.requires[] tokens that were advisory-only: 'qa_passed' requires a case_qa_checklists row with status='passed'; 'recovery_completed'/'recovery_outcome' require a case_recovery_attempts row with non-null result — so a case can no longer reach completed/delivered without a passed QA and a recorded recovery. Entering 'delivered' now stamps cases.actual_completion=now() (never written before). log_case_checkout no longer raw-UPDATEs status='Delivered' (the BEFORE-UPDATE guard would block that anyway); it routes through transition_case_status (enforces the completed/ready→delivered edge + gate; emits the customer phase_changed notification on delivery). Added CHECK constraints locking the vocabularies the gate reads: case_qa_checklists.status IN (pending/in_progress/passed/failed); case_recovery_attempts.result IN (success/partial/failed/no_data). Verified RED→GREEN via authenticated-role simulation (recovery→completed 23514-blocked without evidence; allowed with a recorded attempt; checkout from completed → Delivered + actual_completion set). Function + CHECK only → database.types.ts unchanged. NOTE: requires the QA/recovery capture UI (next C3 sub-unit) before the PR opens — enforcement has no input form yet; 0 cases currently in completable phases so nothing in-flight is blocked. | |
| 20260601074003 | portal_identity_helpers_tenant_resolution | RLS-only | Portal-identity foundation 1/2 (step 5). New get_current_portal_customer_id() reads the customer_id JWT claim (NULL for staff/anon). get_current_tenant_id() made portal-aware: resolves staff via profiles FIRST, portal tenant_id claim only as fallback, NULL when neither (fail-closed) — behaviourally identical for existing staff/anon (verified: owner still resolves its tenant; portal helper NULL with no claim). INERT until the future portal-login edge function issues a role='portal' JWT. EXECUTE on the portal helper revoked from anon. Function-only → database.types.ts unchanged. | |
| 20260601074108 | portal_role_and_customer_scoped_read_policies | RLS-only | Portal-identity foundation 2/2 (step 5). New dedicated `portal` Postgres role (default-deny — inherits NO TO authenticated policy), granted to authenticator + SELECT on cases/case_reports/quotes/invoices/payments/case_communications/case_devices/case_portal_visibility. TO portal SELECT policies scope each table to get_current_portal_customer_id() (direct customer_id on cases/invoices/payments/quotes; case_id→cases.customer_id join on the rest). Verified RED→GREEN by portal-role simulation: customer X sees only X's case (1), ZERO of another customer's case in the same tenant (anti-leak), permission-denied on ungranted staff tables. INERT until a portal JWT exists. No table/column/type change → database.types.ts unchanged. Edge-function JWT minting + frontend cutover = follow-up PR (step 4 of docs/portal-identity-design.md). | |
| 20260601080950 | c4_recovery_manifest_tables | Additive (3 tables) | C4 recovered-file manifest data layer — the lab's defining deliverable artifact, as typed tables (NOT case_reports JSONB) so totals/acceptance are DB-enforceable. recovery_manifests (per-case header: title/source[tool_export\|manual\|scan]/tool_name/status[draft\|finalized]/total_files/total_folders/total_bytes/finalized_at); recovery_manifest_items (path/name/item_type[file\|folder]/size_bytes/modified_at[text, raw export value]/checksum, optional device_id); recovery_manifest_acceptances (APPEND-ONLY legal record — accepted_by_type[staff\|customer]/acceptance_method[staff_attested\|portal]/accepted_by_name; UPDATE+DELETE REVOKEd from authenticated/anon). All three: tenant_id NOT NULL FK + ENABLE/FORCE RLS + RESTRICTIVE tenant_isolation + is_staff_user() write policies + admin DELETE + set_*_tenant_and_audit trigger + idx_*_tenant_id partial index. database.types.ts regenerated (+199 lines, additive only). Verified via authenticated-role sim: create case→manifest→2 items→staff acceptance→finalize (items=2, acceptances=1, status=finalized); acceptance UPDATE 42501-denied (append-only). Ingestion parser (src/lib/manifestIngest.ts, 13 tests) parses CSV/JSON tool exports. Follow-up: manifest UI tab + PDF + make manifest-accepted a release-gate precondition (C3 integration). | |
| 20260601092707 | atomic_record_and_void_payment_rpcs | Function (atomicity/integrity) | Financial Phase 1 — atomic money RPCs replacing the non-atomic multi-write TS path. record_payment(p_payment jsonb, p_allocations jsonb) and void_payment(p_payment_id uuid): SECURITY DEFINER, single transaction, lock target invoices FOR UPDATE. record_payment enforces amount>0, allocations non-empty, each allocation>0 and <= invoice.balance_due, same-currency invariant (payment.currency = invoice.currency; mixed-currency deferred to Phase 2 FX), and Σ(allocations)=payment.amount (money conservation) — then recomputes amount_paid/balance_due/status (+_base) and posts ONE income financial_transactions row. void_payment reverses each invoice balance, soft-deletes allocations, and posts a REVERSING (negative) income row instead of deleting the original (ledger stays append-only). Both set created_by=auth.uid() server-side. Also hardened get_next_number with FOR UPDATE row-lock (duplicate-number race) and added _fin_base_currency/_fin_currency_decimals helpers. Frontend paymentsService.createPayment/voidPayment repointed to the RPCs; non-atomic allocatePaymentToInvoices (+ JS rollback) removed. database.types.ts regenerated (functions added). Verified RED→GREEN against live DB (happy path + void reversal rolled back; 5 validation branches rejected: over-allocation, conservation mismatch, zero amount, currency mismatch, empty allocations). | |
| 20260601094304 | phase2_financial_integrity_constraints | Additive (CHECK + indexes) | Financial Phase 2 — DB-level backstops so the record_payment/void_payment guarantees hold against ANY writer. payment_allocations & receipt_allocations: CHECK(amount>0) + partial UNIQUE(payment_id|receipt_id, invoice_id) WHERE deleted_at IS NULL + FK indexes. payments: CHECK(amount>0) + partial UNIQUE(tenant_id, payment_number) + invoice_id index. invoices: CHECK(amount_paid>=0), CHECK(balance_due>=0), partial UNIQUE(tenant_id, invoice_number) (tax-document integrity). Pre-flight verified zero existing violations → all added validated. CHECK constraints + indexes do not surface in generated types → database.types.ts unchanged. Deferred to Phase 3+: invoice sum-equality invariant (fragile while invoiceService hand-rounds), status CHECK enum (label-drift reconciliation), quote/receipt number uniqueness. | |
| 20260601102523 | add_user_sidebar_position_column | Additive | Per-user navigation sidebar placement — user_sidebar_preferences.sidebar_position text NOT NULL DEFAULT 'left' CHECK (left\|right). Table is already per-user (unique user_id index) + RESTRICTIVE tenant isolation + set_*_tenant_and_audit trigger, so this is a column-only add (no isolation/trigger/index work); the 2 existing rows backfill to 'left' (prior hardcoded behaviour). database.types.ts regenerated (additive). | |
| 20260601102958 | financial_phase3_ledger_lockdown_and_manual_txn_rpcs | Function + RLS/REVOKE/trigger (append-only) | Financial Phase 3 — closes the audit's CRITICAL "mutable ledger" finding by making financial_transactions append-only and routing all manual writes through SECURITY DEFINER RPCs. Lockdown: DROP the permissive financial_transactions_update/_delete RLS (was USING(true)/CHECK(true)), REVOKE UPDATE/DELETE/TRUNCATE from authenticated+anon, add prevent_audit_mutation BEFORE UPDATE/DELETE trigger (INSERT-exempt, so definer-RPC inserts incl. record_payment/void_payment are unaffected). New post_manual_transaction(jsonb): atomic manual entry — validates type∈income/expense/asset/equity + amount>0, sets created_by=auth.uid() server-side, maintains bank_accounts.current_balance in the same tx (FOR UPDATE lock + insufficient-balance guard) — replaces transactionsService.createTransaction's non-atomic insert+JS balance write. New reverse_financial_transaction(uuid, text): append-only correction posting a contra (negated amount/amount_base) row linked via reference_type='reversal'/reference_id, unwinding the bank balance, never mutating the original; rejects double-reversal + reverse-of-reversal — replaces voidTransaction's now-forbidden soft-delete. Frontend: transactionsService createTransaction/voidTransaction repointed to the RPCs; dead updateTransaction/deleteTransaction/createTransactionFrom{Payment,Expense,Invoice} (zero callers; the latter would double-post vs record_payment) + updateBankAccountBalance helper removed; TransactionsList void confirm copy updated (original preserved). Both RPCs REVOKE EXECUTE from anon/PUBLIC, GRANT to authenticated+service_role, search_path pinned. Verified RED→GREEN via authenticated-role simulation (9/9: post/expense/reverse balances, created_by, contra+ref, original preserved, lockdown blocks UPDATE/DELETE/soft-delete, definer reversal still works, 5 validation branches reject). database.types.ts regen DEFERRED to a follow-up commit (the 2 functions must be added via `npm run db:types` before typecheck/schema-drift pass). | |
| 20260601105930 | add_tenants_ui_language_column | Additive | tenants.ui_language text NOT NULL DEFAULT 'en' CHECK (en\|ar) — tenant-wide UI language/text-direction decoupled from country (country still drives currency/date/number formats; this governs only interface language + dir; backfills existing tenants to en/LTR). Applied to the live DB out-of-band by the UI-language workstream; captured on the Phase-3 branch only to unblock PR #137's manifest gate (the manifest check reads the live DB). database.types.ts regen owed alongside the Phase-3 functions. | |
| 20260601143005 | create_receipt_with_allocations | Function (atomicity/integrity) | Phase 5 P1 / F2 — closes the receipt ledger-bypass: receipt recording was a non-atomic multi-write TS path that never posted to the append-only ledger and left invoices.amount_paid stale. New create_receipt_with_allocations(p_receipt jsonb, p_allocations jsonb): SECURITY DEFINER, single transaction, locks target invoices FOR UPDATE; enforces amount>0, allocations non-empty, each allocation>0 and <= invoice.balance_due (no overpayment), base-currency-only (foreign-currency invoices rejected → use the payments path), and Σ(allocations)=receipt.amount (money conservation); recomputes amount_paid/balance_due/status (+_base) and posts ONE income financial_transactions row (reference_type='receipt'). Mirrors record_payment. created_by set server-side; search_path pinned to public; EXECUTE granted to authenticated. Frontend: src/lib/receiptsService.ts wrapper + both invoice-payment callers rewired off the old bypass. database.types.ts regenerated (function added). Verified RED→GREEN against live DB. Pre-merge drift scan (scripts/financial/detect-receipt-ledger-drift.sql) on prod: receipts_missing_ledger=0, invoices_amount_paid_drift=0 (0 receipts pre-fix → no corrective backfill needed). get_advisors: only the by-design authenticated_security_definer_function_executable info on the new RPC; not in function_search_path_mutable. Applied to prod during PR #141 but the manifest row was dropped by the squash-merge → this row restores the migration-manifest gate on main. | #141 |
| 20260602112150 | enforce_single_primary_device | Additive (index + RPCs) + backfill | PR #153 — DB-enforced single-primary device invariant. Backfill demotes all but the earliest (created_at, id tie-break) primary per case (2 dual-primary cases → 0). Partial UNIQUE index idx_case_devices_single_primary ON case_devices(case_id) WHERE is_primary AND deleted_at IS NULL. promote_device_to_primary(p_device_id,p_case_id) SECURITY DEFINER — tenant-validates target + siblings, atomic demote-then-promote, logs device_primary_changed to case_job_history, traps unique_violation (40001). get_primary_device_for_case(p_case_id) selector. database.types.ts regenerated. | #153 |
| 20260602112223 | case_checkout_persistence_and_custody | Additive (columns) + function | PR #153 — checkout PDF read cases.checkout_* columns that never existed (log_case_checkout only logged JSON to case_job_history → blank form). Added cases.checkout_collector_name/_mobile/_id (text), checkout_date (timestamptz), recovery_outcome (text). Rewrote log_case_checkout (dropped 5-arg, added p_device_ids uuid[]): keeps the append-only case_job_history insert, UPDATEs the new cases projection (no status change so the status guard is untouched), writes per-device chain_of_custody_transfers (from_person=performer full_name, to_person=collector, reason checkout, status accepted), then transition_case_status to delivered as before. database.types.ts regenerated. | #153 |
| 20260602112242 | receipts_multicurrency_columns | Additive (columns) + backfill | PR #153 — closed the receipts multi-currency drift (quotes/invoices already had *_base; receipts had none). Added receipts.exchange_rate numeric(10,6) NOT NULL DEFAULT 1, rate_source text NOT NULL DEFAULT 'derived', amount_base numeric(12,3); backfilled amount_base = round(amount*exchange_rate,3). database.types.ts regenerated. | #153 |
| 20260602112313 | case_notes_edit_rls_and_audit | RLS + function | PR #153 — internal-note editing. Tightened case_internal_notes_update RLS from qual=true/check=true (any tenant member) to (created_by = auth.uid() OR has_role('admin')). New update_case_note(p_note_id,p_content) SECURITY DEFINER: tenant-scoped fetch, author-or-admin gate, updates content+updated_at, INSERTs append-only case_job_history note_updated (old/new). database.types.ts regenerated. | #153 |
| 20260603021607 | log_case_checkout_best_effort_delivered_transition | Function (best-effort transition) | PR #161 — device checkout/return rolled back the ENTIRE log_case_checkout RPC for any case not in completed/ready phase: the trailing transition_case_status(...->delivered) raised 23514 (Transition <phase> -> delivered is not allowed; only completed/ready->delivered edges exist) and aborted the transaction, discarding the custody record. Made step (d) best-effort — (a) append-only case_job_history checkout, (b) cases.checkout_* projection, (c) per-device chain_of_custody_transfers all commit; the delivered transition now runs inside BEGIN/EXCEPTION that swallows ONLY check_violation (23514, no active edge from current phase) and insufficient_privilege (42501, role not permitted) and re-raises anything else — so a declined/unrecoverable/awaiting_approval return records custody without forcing an invalid delivered status. Happy path (completed/ready->delivered incl. actual_completion + customer notification) unchanged. Verified RED->GREEN via authenticated-role simulation rolled back (awaiting_approval: custody 0->3, checkout_* written, status unchanged). Function-only CREATE OR REPLACE -> database.types.ts unchanged. | #161 |
| 20260603200217 | persist_quote_invoice_document_fields | Additive (columns + CHECK + index) | Edit-data-loss class fix — Quote/Invoice edit forms collected Title, Client Reference, Discount Type and (quotes) Bank Account but no columns existed (flagged TODO(B8)), so those fields could never round-trip and reloaded blank/reset on edit; discount_type loss also silently reinterpreted a percentage discount as fixed. Added quotes.title/client_reference (text), discount_type (text NOT NULL DEFAULT 'fixed'), bank_account_id (uuid FK bank_accounts ON DELETE SET NULL); invoices.title/client_reference (text), discount_type (text NOT NULL DEFAULT 'fixed'). CHECK (discount_type IN fixed/percentage/amount) on both; partial idx_quotes_bank_account_id WHERE deleted_at IS NULL. Additive/nullable-or-defaulted — existing rows backfill discount_type->'fixed' (prior implicit behavior). database.types.ts regenerated. Callers (quotesService/invoiceService create+update, CaseDetail onSave, edit adapters) wired to persist+return. | |
| 20260607164508 | invoice_payment_status_derived_and_repair | Additive (generated column + column + CHECK + index) + data repair | Payment-workflow Phase 1 (invoices). Added invoices.payment_status text GENERATED ALWAYS AS (paid when amount_paid>=total_amount AND total_amount>0; partial when amount_paid>0; else unpaid) STORED — a drift-proof single source of truth for settlement, replacing permission/edit logic that keyed off the free-text status string written inconsistently by ≥3 paths ('partial' vs 'partially-paid'). Added payments.transaction_id text for the payment-history trail. Unified legacy 'partially-paid'→'partial' and repaired drifted lifecycle status to match amounts (e.g. INVO-0008 was 'paid' with a live balance → 'partial'). Added invoices_status_check CHECK (draft/sent/paid/partial/overdue/cancelled/void/converted) and partial idx_invoices_payment_status WHERE deleted_at IS NULL. database.types.ts regenerated. Editability + record-payment now derived from facts via src/lib/invoicePermissions.ts. | |
| 20260607193758 | add_tenants_feature_flags | Additive (column + function) | Tenant Feature Management — per-tenant UI feature toggles. Added tenants.feature_flags jsonb NOT NULL DEFAULT '{}' (override-only; empty => all code-registry defaults => existing tenants unchanged). Added tenant_feature_enabled(p_tenant_id uuid, p_key text) RETURNS boolean SECURITY DEFINER STABLE, search_path=public, COALESCE((feature_flags->>key)::boolean, true) so a missing key/row fails OPEN; EXECUTE granted to authenticated+anon for the customer-portal login gate. Frontend: src/lib/features/{registry,resolveFeatures}.ts + TenantConfig.featureFlags (loaded in tenantConfigService) + useTenantFeature; enforced on case tabs, stage banner, dashboard widgets, sidebar nav, customer portal (PortalLayout + PortalAuthContext server gate), and FeatureRoute. database.types.ts regenerated via the Supabase MCP type generator (CLI unavailable in-container); temporary casts removed. | #177 |
| 20260608070059 | add_convert_proforma_invoice_to_tax_invoice_rpc | Function (atomic conversion) | Proforma INVOICE -> Tax INVOICE conversion (was 100% broken). CaseDetail's "Convert to Tax Invoice" (on a proforma invoice) and InvoiceDetailPage.handleConvertToTax both sent the INVOICE id to convert_proforma_to_tax_invoice(p_quote_id), which reads the quotes table -> RAISE 'Quote not found' -> 400; the CaseDetail modal also read quote fields off an invoice (Quote Number/Customer = N/A). New convert_proforma_invoice_to_tax_invoice(p_invoice_id uuid, p_due_date timestamptz, p_notes text) RETURNS uuid SECURITY DEFINER search_path=public: tenant-scoped, validates source is an unconverted proforma, creates a linked tax_invoice (copies header + line items, sets proforma_invoice_id), marks the proforma status='converted' + converted_to_invoice_id, atomically. EXECUTE to authenticated. Frontend: invoiceService.convertProformaToTaxInvoice now calls this RPC with p_invoice_id (fixes both call sites); new ConvertProformaToTaxModal (shows invoice number/customer/amount, no type selector) replaces the mis-used quote ConvertToInvoiceModal in CaseDetail (QuoteDetailPage keeps the quote modal). database.types.ts regenerated. Verified RED->GREEN against the live DB (rolled back). | |
| 20260609191824 | add_payroll_social_security_rate | Additive (column + CHECK) | Platform audit (B1) — payrollService.processPayroll hardcoded socialSecurityDeduction = basicSalary*0.07 for all tenants and discarded the fetched overtime (void dailyRate/hourlyRate; total_earnings=basicSalary; overtime_amount never populated). Added payroll_settings.social_security_rate numeric NOT NULL DEFAULT 0.07 (CHECK 0..1), per-tenant configurable; default preserves prior Oman PASI behavior. Service reads it via parsePayrollSettings, pays overtime = hourlyRate*overtime_hours*overtime_rate_multiplier.regular into total_earnings + payroll_records.overtime_amount, and posts loan repayments AFTER the payroll_records insert (was before -> partial failure left loans deducted with no record). database.types.ts updated additively. | audit |
| 20260609195921 | stock_atomic_mutation_rpcs | Additive (CHECK + RPCs) | Atomic stock mutation RPCs replacing non-atomic browser-side write sequences for stock sales, receipts, and adjustments — record_stock_sale(p_sale jsonb, p_items jsonb), record_stock_receipt(p_item_id, p_quantity, p_options jsonb), record_stock_usage_for_case(...), post_stock_adjustment(p_session_id, p_approved_by). Each is SECURITY DEFINER, single transaction, FOR UPDATE row-lock on the stock_items balance, non-negative on-hand guard (raises on oversell), writes the stock_transactions ledger row and flips serials when provided (mirrors the record_payment / create_receipt_with_allocations atomic pattern). Also adds stock_items_quantity_on_hand_nonneg CHECK (quantity_on_hand >= 0) NOT VALID. Key schema fact: current_quantity/quantity_available are GENERATED from quantity_on_hand — RPCs write quantity_on_hand. Applied to the live DB out-of-band by the stock/inventory workstream; manifest row captured on PR #181 to satisfy the migration-manifest gate. database.types.ts regenerated in the same commit. | |
| 20260609200015 | add_inventory_case_assignment_custody_rpcs | Additive (RPCs) | Atomic, custody-logged inventory/stock ↔ case assignment — fixes the CRITICAL hole where assigning a physical inventory item to a case logged NO chain-of-custody event and never reserved the item (one unique asset could be "assigned" to multiple cases). assign_inventory_to_case(p_item_id, p_case_id, p_notes) / unassign_inventory_from_case: ONE transaction — FOR UPDATE lock + tenant guard, double-assign guard (active = returned_at IS NULL), inventory_case_assignments insert, status flip to "In Use", chain_of_custody event (device_id NULL — inventory parts are not case_devices; item id rides in metadata), case_job_history + audit trail via existing log_* helpers. SECURITY DEFINER, search_path=public, tenant via get_current_tenant_id(). Applied out-of-band; manifest row captured on PR #181. | |
| 20260609200115 | payment_before_release_gate_in_transition | Function (opt-in release gate) | Platform-audit critical — adds a tenant opt-in "payment before release" gate to the governed case state machine (lifecycle stage 14→13 anti-pattern: no payment-before-release gate). When tenants.feature_flags->>'gate.payment_before_release' is explicitly true, transition_case_status blocks entry into the delivered phase while any non-proforma invoice on the case has balance_due > 0. Default OFF via COALESCE(...,false) — zero behavioral change for existing tenants. Same 4-arg signature; one new IF block after the existing C3 qa/recovery gates. Applied out-of-band; manifest row captured on PR #181. | |
| 20260609200206 | fix_record_stock_usage_for_case_quantity_on_hand | Hotfix (function) | record_stock_usage_for_case read/wrote current_quantity, which is a GENERATED STORED column (= quantity_on_hand) — the write was invalid. Function now gates on and decrements the writable quantity_on_hand (current_quantity follows); keeps the FOR UPDATE lock, tenant guard, quantity > 0 and insufficient-stock checks. Applied out-of-band; manifest row captured on PR #181. | |
| 20260609200311 | fix_inventory_custody_rpcs_audit_overload_cast | Hotfix (function) | log_audit_trail has 6-arg and 8-arg overloads; the inventory custody RPCs' bare NULL final argument made the call ambiguous. Re-applies assign_inventory_to_case / unassign_inventory_from_case casting p_changed_fields to text[] to bind the 6-arg overload. Superseded by 20260609200421 (named-args binding). Applied out-of-band; manifest row captured on PR #181. | |
| 20260609200421 | fix_inventory_custody_rpcs_audit_named_args | Hotfix (function) | Both log_audit_trail overloads share identical first-6 signatures, so the 20260609200311 cast was still irreducibly ambiguous. Re-applies the inventory custody RPCs binding the 8-arg overload explicitly via named args (p_ip_address / p_user_agent), which is unambiguous. Applied out-of-band; manifest row captured on PR #181. | |
| 20260609230422 | pin_search_path_on_enforcement_trigger_functions | Hardening (function config) | Audit SEC-3 (advisor function_search_path_mutable, 3 findings) — ALTER FUNCTION ... SET search_path = public, pg_temp on prevent_audit_mutation (append-only audit/custody enforcement), guard_cases_status_changes (case status state machine), set_clone_drive_retention_deadline. Mutable search_path on the enforcement triggers was a hijack vector for the platform's forensic guarantees. Config-only, no body/behavior change → database.types.ts unchanged (verified byte-identical). | audit |
| 20260609230557 | add_fk_covering_indexes_case_custody_financial | Additive (44 indexes) | Audit PERF-1 (advisor unindexed_foreign_keys; 93 on case/custody tables) — covering indexes for FK columns backing real query paths: case_id on all 11 case child tables + 3 custody child tables, device_id on custody/diagnostics/recovery tables, chain_of_custody.actor_id, access_log.custody_entry_id, line-item parents (invoice_line_items.invoice_id, quote_items/case_quote_items.quote_id, case_report_sections.report_id), quotes (case/customer/status/company), invoices (company/status/created_by), payments (customer/payment_method/bank_account), cases (assigned_to/company/created_by/priority), customers_enhanced (customer_group/country), case_devices.device_type_id, case_engineers.user_id. Cold catalog FKs on case_devices deliberately deferred (write amplification vs. no read path). All CREATE INDEX IF NOT EXISTS, plain (non-partial) so they also serve FK-constraint checks → database.types.ts unchanged. | audit |
| 20260609230829 | revoke_anon_execute_on_security_definer_functions | Hardening (REVOKE/GRANT) | Audit SEC-1 (advisor anon_security_definer_function_executable, 72 findings) — Postgres default PUBLIC EXECUTE made destructive/business SECURITY DEFINER RPCs callable pre-auth. REVOKE EXECUTE FROM PUBLIC, anon + explicit GRANT to authenticated, service_role on 49 functions incl. delete_case_permanently, set_portal_password/set_customer_portal_password, disable_customer_portal_access, export/anonymize_customer_data, record/void-money RPCs' helpers, log_* writers, stock/inventory RPCs, get_next_* generators, update_number_sequence. invoke_sync_exchange_rates special-cased to service_role only (restores 20260529203637 intent; original revoke missed the PUBLIC grant). KEPT anon-callable (verified live anon-key frontend callers): authenticate_portal_customer, tenant_feature_enabled, change_portal_password, approve_quote, reject_quote (PortalQuotes.tsx runs on the anon key until the portal-JWT cutover; migrate these to the `portal` role then). RLS helper functions (is_*/has_*/get_current_*/get_my_*/belongs_to_*) untouched — they evaluate inside policies for pre-auth requests. Verified post-revoke: only the 5 intentional functions remain anon-executable. Grants don't surface in generated types → database.types.ts unchanged. | audit |
| 20260609230939 | restrict_public_bucket_listing_to_authenticated | Hardening (storage RLS) | Audit SEC-6 (advisor public_bucket_allows_listing, 4 findings) — the "Public users can view ..." SELECT policies on storage.objects applied to ALL roles, letting anonymous clients enumerate every file in customer-profile-photos and staff-profile-avatars (cross-tenant customer/staff PII filenames) plus company-assets/company-qrcodes via the list API. Replaced each with an authenticated-scoped SELECT policy. Buckets stay public=true: rendering uses getPublicUrl direct object GETs which bypass storage RLS, and the frontend never calls .list()/.download() on these buckets (verified) → zero app-visible change. database.types.ts unchanged. | audit |
| 20260609233520 | complete_forensic_append_only_lockdown | Hardening (REVOKE + trigger + RLS) | Audit SEC-2 (advisor rls_policy_always_true on forensic tables) — completed the append-only contract on all 8 forensic tables. case_job_history and number_sequences_audit were the open holes (no prevent_audit_mutation trigger, live authenticated UPDATE/DELETE grants behind USING(true) policies — any authenticated user could rewrite case history): REVOKE UPDATE/DELETE/TRUNCATE from authenticated+anon, added prevent_audit_mutation BEFORE UPDATE OR DELETE triggers, dropped the policies. Also dropped the dead USING(true) UPDATE/DELETE policies on the 6 already-revoked tables (chain_of_custody, _access_log, _integrity_checks, _transfers, audit_trails, financial_audit_logs) so a future re-GRANT cannot silently re-open mutation. Frontend is insert/select-only on both open tables (verified); definer writers (log_case_history, get_next_number) run as postgres (bypassrls) — unaffected. KNOWN FOLLOW-UP: chain_of_custody_transfers accept/reject does a direct .update() in chainOfCustodyService (lines ~532/572) that was ALREADY failing 42501 (grant revoked before this migration); needs a SECURITY DEFINER accept_custody_transfer RPC. database.types.ts unchanged. | audit |
| 20260609233535 | lock_signup_otps_to_service_role | Hardening (RLS + REVOKE) | Audit SEC-5 (advisor rls_policy_always_true, anon) — signup_otps had anon INSERT WITH CHECK(true) + anon UPDATE USING(true) + authenticated FOR ALL USING(true): unauthenticated OTP flooding/forgery/enumeration surface against tenant provisioning. Dropped all three policies and REVOKEd ALL from anon+authenticated. All legitimate OTP traffic goes through the send-otp-email edge function on the service role (verified: tenantService.sendOtp/verifyOtp only call the edge function; nothing in src touches the table). service_role bypassrls → edge flow unaffected. database.types.ts unchanged. | audit |
| 20260609234324 | role_gate_always_true_write_policies | RLS hardening (generated, 318 policies) | Audit SEC-4 (advisor rls_policy_always_true: 319 findings / 162 tables; signup_otps handled separately in SEC-5) — every remaining INSERT WITH CHECK(true) / UPDATE USING(true) policy granted TO authenticated rewritten with a role gate via a catalog-driven DO block. Buckets: financial money/banking/VAT tables (invoices, invoice_line_items, payments+allocations/receipts/disbursements, receipts+allocations, expenses+attachments, bank_*, account_*, reconciliation_matches, tax_rates, vat_*) -> (SELECT has_role('accounts')); HR & payroll (departments, positions, employees*, attendance, timesheets, leave_*, salary_components, payroll_*, performance_reviews, onboarding_*, recruitment_*) -> (SELECT has_role('hr')); profiles + tenants -> (SELECT is_platform_admin()) (created only via service_role edge functions / definer triggers — verified no direct frontend inserts); all other tenant tables (incl. quotes/case workflow — sales/technician work) -> (SELECT is_staff_user()). Closes the live hole where 3 active viewer-role accounts could write cases/invoices/payments. Helpers are SECURITY DEFINER STABLE with pinned search_path, wrapped in (SELECT ...) against auth_rls_initplan. postgres/service_role have bypassrls (verified) so definer RPCs and edge functions are unaffected; portal anon/portal roles untouched. Verified post-apply: 0 always-true authenticated write policies remain; spot checks cases_update=is_staff_user, invoices_insert=has_role(accounts), payroll_records_insert=has_role(hr), profiles_insert/tenants_insert=is_platform_admin, quotes_insert=is_staff_user. database.types.ts unchanged. | audit |
| 20260409000000 | baseline_schema | Baseline adoption (registry only) | Registered the repo's 2026-04-09 full-schema baseline file (supabase/migrations/20260409000000_baseline_schema.sql, 9,365 lines) as ALREADY APPLIED on prod via direct schema_migrations insert. The file exists so Supabase preview branches can bootstrap a fresh database; prod's schema predates and supersedes it (built via MCP migrations). Without this registration, any merge touching supabase/ could make the GitHub integration replay the entire unguarded baseline (226 bare CREATE TABLEs) against production. No schema change on prod. | preview-fix |
| 20260409000001 | populate_country_phone_codes | Baseline adoption (rename + registry) | The phone-codes data migration (formerly 20260407100420) sorted BEFORE the 20260409000000 baseline, so every fresh preview branch died at statement 0 ("relation geo_countries does not exist") — the visible Supabase Preview CI failure on PRs touching supabase/. Renamed to 20260409000001 so it replays after the baseline, and registered as applied on prod (all 58 active geo_countries rows already carry phone_code). Idempotent UPDATEs only. | preview-fix |
| 20260610043346 | fix_log_case_communication_sent_to_and_service_role | Function (logging repair) | Templates/Reports epic P0 — case communication logging was silently broken: send-document-email (edge fn) and whatsappUtils.logWhatsAppCommunication call log_case_communication with p_sent_to, which the live 5-arg signature lacked → PostgREST PGRST202 ("function not found"), swallowed by callers' try/catch → case_communications permanently 0 rows. Replaced with a 7-arg version (+p_sent_to text, +p_sent_by uuid): derives tenant_id from the case row (the edge fn calls via service role where auth.uid()/get_current_tenant_id() are NULL — the old body would have inserted NULL tenant and been rejected by the tenant trigger anyway), guards user-context callers to their own tenant (platform admins exempt), writes sent_to/sent_by (sent_by falls back to auth.uid()), and wraps the insert with the established transaction-local app.bypass_tenant_guard GUC. EXECUTE revoked from PUBLIC/anon, granted to authenticated + service_role. send-document-email redeployed (v26) passing p_sent_by = validated user.id. database.types.ts regenerated (function args). | |
| 20260610043412 | repair_master_template_codes_and_seed_variables | Data repair (master data) | Templates/Reports epic P0 — the Templates module was dead against live data: master_template_types.code was NULL on all 7 rows (every code-based lookup — TemplatesDashboard type links, TemplateTypeDetail, QuoteFormModal/InvoiceFormModal terms .eq('code',…), seedService.seedTemplates — returned nothing), category_id NULL on all rows despite 6 categories existing, and master_template_variables was empty (RLS only lets platform admins write master_* tables, so tenant-side seeding could never populate it). Populated the 7 codes (quote_terms/invoice_terms/email/sms/whatsapp/service_report/diagnostic_findings) + category links by name, added partial UNIQUE index idx_master_template_types_code, and seeded the 34-variable catalog (company/customer/case/device/quote/invoice/service/technician keys mirroring src/config/seedData.ts templateVariables) with ON CONFLICT (name) DO NOTHING. Data + index only → no type-shape change. | |
| 20260610044443 | document_templates_document_type_and_channel_dedup | Additive (column + CHECK + index) + master-data deactivation | Templates/Reports epic P1 — (a) document_templates.document_type text NULL + CHECK over the 9 emailable DocumentType values + partial lookup index (tenant_id, template_type_id, document_type) WHERE is_active: all 9 email document types previously shared the single 'email' master type code, so per-document default templates were impossible; the new documentTemplatesService default-resolution chain prefers a default pinned to the document type, then a generic default, then the hardcoded emailTemplates.ts fallback. (b) Deactivated master_template_types 'service_report' and 'diagnostic_findings' (dedup: they duplicate the live report_section_presets snippet system and had zero consumers; rows/data preserved, deactivate-don't-delete). database.types.ts regenerated (+document_type on Row/Insert/Update). | |
| 20260610050104 | report_definition_tenant_overrides_and_pdf_bucket | Additive (columns + RLS rewrite + storage bucket) | Templates/Reports epic P2 — (a) tenant overrides for report definitions: master_case_report_templates, report_section_library, report_section_presets, report_template_section_mappings gain tenant_id uuid NULL (NULL = shared system row) + partial indexes; SELECT policies rescoped from USING(true) to (tenant_id IS NULL OR own tenant OR platform admin) so tenant rows are invisible cross-tenant; INSERT/UPDATE/DELETE rescoped from is_platform_admin()-only to also allow tenant admins on own-tenant rows (presets: any staff may insert/update — technicians author snippets from the report editor; system rows stay platform-only). This un-deadens the Settings → Report Sections CRUD, which silently failed under RLS for every tenant admin, and enables tenant report templates with tenant-beats-system default resolution (reportsService.getDefaultTemplate ordering + clone-to-tenant). (b) Private storage bucket case-report-pdfs + INSERT (staff, own-tenant path prefix {tenant_id}/...) and SELECT (own tenant or platform admin) policies, NO UPDATE/DELETE policies — the PDF artifact persisted at send time is immutable evidence of what was released (stages 12/16); reportPDFService.persistReportPDF stamps generated_at/generated_by + content.pdf_file_path and ReportViewModal persists BEFORE flipping status to sent. database.types.ts regenerated (+tenant_id on the four tables). | |
| 20260610054150 | case_follow_ups_scheduling | Additive (columns + CHECK + index) + function + cron + seed | Templates/Reports epic P5 — wires the never-used case_follow_ups table into a working scheduler. Columns: template_id (provenance), channel CHECK(internal/email), send_to, auto_send, subject, message (email content rendered from the template and FROZEN at scheduling time — the author previews exactly what goes out; no Deno-side renderer needed), sent_at, attempt_count, last_error, quote_id; status CHECK(pending/sent/completed/cancelled/failed); partial due-index (tenant_id, follow_up_date) WHERE pending. process_due_case_follow_ups(): SECURITY DEFINER scanner (FOR UPDATE SKIP LOCKED, limit 50) — internal channel emits a case.follow_up_due notification_event (day-bucket dedup; rides the existing dispatch/subscriptions/log/DLQ stack) and completes the row; auto-send email rows claim attempt_count++ then net.http_post the new process-scheduled-followups edge function (graceful skip + last_error when edge_function_base_url/_service_key settings are unpopulated — same dormant-until-configured posture as the exchange-rate job); >=3 attempts → failed. EXECUTE service_role-only. cron.schedule process-case-follow-ups */15min. Seeded NULL-tenant in_app + email notification_templates for case.follow_up_due. Edge fn process-scheduled-followups v1 deployed (service-key auth; sends frozen subject/message via SMTP; sent_at + log_case_communication on success; retry-aware failure handling). database.types.ts regenerated. Verified live: scanner returns ok with 0 due rows; cron.job row present. | |
| 20260610055243 | seed_system_report_definitions_and_recovered_files_type | Data seed (system master data) | Templates/Reports epic P6 — the ENTIRE report-definition dataset was empty on live (0 templates / 0 sections / 0 mappings): creating any case report failed at getDefaultTemplate ("No template found") — the Reports module was dead-on-live, same class of issue as the NULL template-type codes fixed in P0. Seeds (idempotent, NULL-tenant system rows): 11-section library (executive_summary, device_information, initial_assessment, security_analysis, work_performed, recovery_results, recovered_files_summary, findings, chain_of_custody_notes, destruction_certificate, recommendations — lowercase category vocabulary matching the Studio UI), one default template per report type (the 7 existing types + the NEW 'recovered_files' type — the stage-12 recovered-file manifest report), and 35 ordered section mappings (first two sections required). Frontend: ReportType union + REPORT_TYPES gain recovered_files (Files icon); StreamlinedReportEditor prefills recovered_files_summary from manifestService.listManifests totals (HTML-escaped). Portal acknowledgement DEFERRED to the portal-JWT cutover: an acknowledgement recorded via the anon-key portal session would be evidentially weak; recovery_manifest_acceptances already covers staff-attested acceptance. Data-only → no type-shape change. | |
| 20260610080118 | custody_transfer_response_rpc_and_guard | Function + trigger swap (governed transition) | SEC-2 follow-up — custody-transfer accept/reject was impossible: chain_of_custody_transfers carried the blanket prevent_audit_mutation trigger + revoked UPDATE grants, but the table is a STATE MACHINE (initiated/pending_acceptance → accepted/rejected); the immutable forensic record lives in the chain_of_custody EVENT log. Replaced the blanket trigger with guard_custody_transfer_response(): still blocks DELETE and all client UPDATEs; permits ONLY the pending→resolved transition with response fields when the transaction-local app.allow_custody_transfer_response flag is set (app.bypass_* precedent, 20260530051557) — historical fields (parties/case/reason/created_at) DB-immutable, enforced column-by-column. New respond_to_custody_transfer(p_transfer_id, p_action accept|reject, p_payload) SECURITY DEFINER RPC: staff+tenant guards, FOR UPDATE lock, status guard, server-side notes JSON merge (preserves packed seal/condition fields + legacy _raw), rejection_reason required to reject, logs CUSTODY_TRANSFER_ACCEPTED/REJECTED via log_chain_of_custody. EXECUTE: authenticated+service_role only. Frontend accept/rejectCustodyTransfer rewired to the RPC (client-side fetch/merge/update/log removed). Verified RED→GREEN via rolled-back authenticated-role sim: insert ok, direct UPDATE 42501-blocked, accept merges notes + stamps accepted_at + logs custody event (0→1), double-respond and reject-after-resolve check_violation-blocked. database.types.ts regenerated (additive: respond_to_custody_transfer + out-of-band process_due_case_follow_ups & follow-up/report columns). | |
| 20260610094542 | fix_proforma_conversion_number_status_balance | Function + data repair + hardening | Proforma→Tax conversion audit F1/F2/F3/F6/F7 (Critical) — convert_proforma_invoice_to_tax_invoice inserted tax invoices with NO invoice_number, NO status, and balance_due stuck at the 0 default: blank-numbered legal documents, invisible to status filters, unpayable (record_payment guards allocation<=balance_due), falsely settled in AR, and silently passing the payment-before-release gate; it also dropped title/client_reference/discount_type/terms/tax_rate and could inherit an already-past proforma due date. RPC v2: number assigned at issuance via get_next_number('invoices'), status 'draft', amount_paid 0 / balance_due=total, full header mapping, due_date COALESCE(p_due_date, now()+14d), audit via log_audit_trail (8-arg named binding) + log_case_history. Data repair: the 2 production NULL-number converted invoices renumbered from the tenant sequence (INVO-0012/0013), status draft, balances corrected. Hardening: invoices.status DEFAULT 'draft' + NOT NULL; invoices_number_required CHECK NOT VALID. Verified RED→GREEN via rolled-back authenticated sim (6/6: number/status/balance/mapping/future-due, items+audit rows, proforma linked+converted, double-convert blocked). database.types.ts regenerated (status non-null). NOTE: repaired INVO-0013 (336 OMR) duplicates manually-created INVO-0011 — flagged to owner for void decision. | audit |
| 20260610094717 | enforce_invoice_balance_integrity_trigger | Trigger (invariant) | Conversion audit F2 structural fix — balance_due was free-written and any writer that forgot it produced unpaid-but-zero-balance invoices. payment_status already solved this class by being GENERATED; balance_due cannot be dropped/regenerated (no-DROP rule), so BEFORE INSERT OR UPDATE trigger enforce_invoice_balance forces amount_paid=COALESCE(,0) and balance_due=total-amount_paid on EVERY write from ANY writer. Backfill swept residual drift (0 rows post-repair). Verified via rolled-back sim: forged balance on INSERT forced to total; direct UPDATE drift overridden; amount_paid=200 on 500 → balance 300; 0 drift rows table-wide. database.types.ts unchanged. | audit |
| 20260610094902 | unify_invoice_sequences_and_proforma_series | Sequence merge + seed + REVOKE | Conversion audit F4/F5 — two parallel sequences shared the INVO prefix: scope 'invoice' (burned to 21 by InvoiceFormModal's preview fetch on every open; zero persisted invoices ever used it, verified incl. soft-deleted) vs scope 'invoices' (real series) → guaranteed eventual collision surfacing as INSERT failures via uq_invoices_number_per_tenant. Merged GREATEST high-water into 'invoices' (now 21) and removed the legacy config row (documented soft-delete exception: get_next_number doesn't filter deleted_at, so a soft-deleted row would keep feeding the parallel series). Seeded dedicated 'proforma_invoices' scope (PRO-, padding 4) per invoicing tenant — proformas are not tax documents and no longer consume tax-series numbers (EU VAT Art. 226 / GCC VAT sequential-numbering). Legacy quote-based convert_proforma_to_tax_invoice REVOKEd from authenticated/anon (no UI callers; its numberless INSERT is rejected by invoices_number_required anyway). Frontend: getNextInvoiceNumber(type) routes proforma→'proforma_invoices'; InvoiceFormModal preview no longer consumes numbers. Seeds under app.bypass_tenant_guard. database.types.ts unchanged. | audit |
| 20260610094929 | vat_ledger_on_tax_invoice_issuance | Trigger + backfill (VAT ledger) | Conversion audit F6 — the VAT subsystem had ZERO rows ever; tax pages reported from empty tables. AFTER INSERT OR UPDATE trigger post_invoice_vat_record (SECURITY DEFINER): tax_invoice issuance with tax_amount<>0 posts vat_records(record_type 'sale', vat_amount, vat_rate, tax_period YYYY-MM of invoice_date) — vocabulary matches vatService/VATAuditPage; void/cancel transition posts an append-only CONTRA 'sale' row (negative vat_amount), original never mutated. Proformas post nothing. Backfilled 11/11 historical non-void tax invoices keyed to original issuance periods (app.bypass_tenant_guard). Verified via rolled-back sim (A:issue→1 ledger row, B:void→contra, C:proforma→0, D:conversion→1). Full credit-note documents (own series/PDF/delivery) remain the one deliberately deferred feature — this keeps the books correct meanwhile. database.types.ts unchanged. | audit |
| 20260610122736 | assign_receipt_numbers_trigger_and_backfill | Trigger + backfill (document numbering) | Payment-ledger work surfaced the receipts twin of the invoice-numbering defect: create_receipt_with_allocations inserts receipts WITHOUT receipt_number (3 unnumbered receipts in production, no 'receipts' sequence existed). New BEFORE INSERT trigger assign_receipt_number (SECURITY DEFINER) assigns from the per-tenant 'receipts' sequence (prefix RCPT, padding 4, auto-seeded with ON CONFLICT race guard) for EVERY writer; tenant resolved from NEW.tenant_id — not caller context — so definer RPCs and service-role writers both work; pre-supplied numbers respected. Backfilled the 3 production receipts oldest-first (RCPT-0001..0003) under app.bypass_tenant_guard. Verified: 0 unnumbered receipts remain; rolled-back authenticated sim auto-assigned RCPT-0001 on a fresh tenant. database.types.ts unchanged. | audit |
| 20260610162638 | 20260610162638_custody_ledger_write_paths.sql | Trigger + function (custody write paths) | Platform review item 4 P0/P1 — chain_of_custody had 0 rows DB-wide (all 4 custody tables empty across 21 cases / 29 devices) because no lifecycle write path existed: intake logged nothing, the 10 client logging helpers had zero call sites, and checkout wrote only job-history+transfers. (1) trg_log_device_received_custody AFTER INSERT ON case_devices logs a DEVICE_RECEIVED creation event (custody_status in_custody) — DB-side so no client path (wizard, bulk RAID add, future API) can skip intake custody. (2) log_chain_of_custody p_device_id/p_action_category/p_action gain DEFAULT NULL + explicit required-args guard so PostgREST named calls can omit the device for case-level events — clients previously sent '' which failed the uuid cast (22P02) on every deviceless event; positional caller respond_to_custody_transfer unaffected (identity unchanged via CREATE OR REPLACE). (3) log_case_checkout additionally writes DEVICE_CHECKED_OUT (per device) / CASE_CHECKED_OUT ledger events with custody_status checked_out. Verified live: rolled-back probe INSERT on case_devices produced exactly 1 DEVICE_RECEIVED in_custody event; prevent_audit_mutation confirmed blocking even postgres DELETEs. database.types.ts regenerated (optional RPC args). | review |
| 20260610162702 | 20260610162702_audit_actor_fields.sql | Additive columns + trigger | Platform review item 3 — actor stamping was app-side and almost always missed (updated_by set on only 3 of 28 cases). New set_audit_actor_fields() BEFORE INSERT/UPDATE trigger (INSERT: created_by := COALESCE(created_by, auth.uid()); UPDATE: updated_by := COALESCE(auth.uid(), updated_by) so system jobs keep the prior editor) attached to cases, invoices, quotes, customers_enhanced, companies, case_internal_notes, case_devices. Adds updated_by uuid to case_internal_notes (notes show an "Edited" badge but recorded no editor) and case_devices (corrected post-intake). EXECUTE revoked from anon/authenticated per Phase-6 trigger-function hardening. database.types.ts regenerated. | review |
| 20260610162720 | 20260610162720_single_primary_company_per_customer.sql | Data fix + partial unique index | Platform review item 1 — ALL 9 linked customers had no primary company (createCustomer inserts is_primary=false), so CreateCaseWizard's primary-company auto-fill and ClientTab's fallback picked an arbitrary relationship row. Marks the earliest active relationship primary per (tenant, customer), then enforces single-primary via partial unique index uq_customer_primary_company (tenant_id, customer_id) WHERE is_primary AND deleted_at IS NULL. Pre-checked 0 multi-primary conflicts; verified 9 primaries set post-migration. Frontend follow-up in the same change-set: createCustomer links first company as primary; ManageCompaniesModal manages add/end/set-primary with audit_trails entries. | review |
| 20260610162738 | 20260610162738_custody_baseline_for_existing_devices.sql | Data backfill (append-only ledger) | Platform review item 4 — devices that predate the custody write paths get ONE clearly-labelled retroactive CUSTODY_BASELINE_ESTABLISHED critical_event (custody_status in_custody, actor System, metadata.retroactive=true, NOT EXISTS-guarded so devices with real events are skipped). History is not fabricated: the description states custody tracking begins at this entry. Runs under app.bypass_tenant_guard (no auth context). Verified live: 30/30 active devices now have a custody row; devices_without_custody = 0. | review |
| 20260611233530 | sidebar_low_stock_count_fn | Function (count helper) | Performance pass (PR #197) — get_low_stock_count(): SECURITY INVOKER (tenant RLS applies) STABLE count of active, non-deleted stock_items with current_quantity <= minimum_quantity. PostgREST cannot express a column-to-column comparison filter, so the sidebar badge previously fetched EVERY active stock row on each 2-minute poll just to count low-stock client-side. EXECUTE revoked from public/anon, granted to authenticated/service_role. useSidebarBadges.ts rewired to the RPC. database.types.ts regenerated (+1 line: the RPC signature). | perf |
| 20260611234736 | rls_initplan_wrap_auth_uid | Policy ALTER (initplan) | Performance pass (PR #197) — advisor auth_rls_initplan (12 WARNs, 0 after): wrapped bare auth.uid() in (select auth.uid()) via ALTER POLICY (no drop window) on profiles_select/_tenant_isolation/_update, announcement_dismissals_insert/_select, case_internal_notes_update, notification_subscriptions_select/_insert/_update/_delete, notification_log_select/_update. Expressions otherwise verbatim — semantics identical; the subselect is hoisted to an initplan and evaluated once per statement instead of per row. profiles matters most: every RLS helper and every AuthContext profile fetch reads it. | perf |
| 20260612002013 | dedupe_permissive_select_policies | Policy split (dedupe) | Performance pass (PR #197) — advisor multiple_permissive_policies (11 WARNs, 0 after): each table had a FOR ALL manage/write policy alongside a FOR SELECT read policy, OR-ing two permissive policies on every row read. Split the manage policies into FOR INSERT/UPDATE/DELETE with identical expressions on billing_coupons, billing_invoice_items, case_status_transitions, exchange_rates, notification_templates, notification_webhooks, plan_features, platform_announcements, subscription_plans, tenant_rate_limits, tenant_sla_policies. Semantics preserved — every read policy already covered what the manage policy granted for SELECT, except tenant_rate_limits_select which was widened to ((deleted_at IS NULL) OR is_platform_admin()) to keep platform-admin visibility of soft-deleted rows. subscription_plans_anon_select (anon) and all RESTRICTIVE tenant-isolation policies untouched. Hot-path FK indexes were evaluated and intentionally NOT added: all 317 unindexed-FK findings are on cold/near-empty tables, and 164 unused indexes already exist. | perf |
| 20260613120304 | add_invoices_credited_amount | Additive (columns + CHECK) | Phase A.1 credit notes — adds invoices.credited_amount + credited_amount_base (NOT NULL DEFAULT 0) with a non-negative CHECK. balance_due = total_amount - amount_paid - credited_amount; the generated payment_status and record_payment/void_payment are updated in 20260613120627 to subtract it. | 199 |
| 20260613120355 | create_credit_notes_tables | Additive (3 tenant tables) | Phase A.1 — credit_notes (header), credit_note_items (lines), credit_note_allocations (append-only apply-to-invoice). Full tenant package per table: RLS enabled+forced, RESTRICTIVE *_tenant_isolation, permissive select/insert/update/delete (accounts/admin) mirroring payment_allocations, set_*_tenant_and_audit trigger, idx_*_tenant_id partial index; uq_credit_notes_number_per_tenant, uq_credit_note_alloc_active. | 199 |
| 20260613120513 | credit_note_rpcs | Functions (SECURITY DEFINER) | Phase A.1 — issue_credit_note (header+items; revenue↓ to financial_transactions + output VAT reversal to vat_transactions; reason required), apply_credit_note (append credit_note_allocations; adjustment/refund/writeoff → invoices.credited_amount, advance_adjustment → amount_paid; recompute balance_due/status; Σ ≤ remaining credit and ≤ invoice balance), void_credit_note (reverse allocations + ledger + VAT, mark void; blocked once refunded). REVOKE public/anon, GRANT authenticated. | 199 |
| 20260613120627 | payment_rpcs_credited_amount | Function replace + generated column | Phase A.1 — record_payment/void_payment now compute v_new_due = total - amount_paid - credited_amount and count credits toward 'partial' (otherwise identical to the live bodies). Dropped + re-added the generated invoices.payment_status so its expression includes credited_amount, and recreated idx_invoices_payment_status (no view/policy depended on it). Verified live via rolled-back probe (credited=total ⇒ payment_status=paid). | 199 |
| 20260613164410 | pdf_template_engine_m1_schema | Additive (3 tenant tables + pins + RLS) | PDF Template Engine M1 (spec docs/superpowers/specs/2026-06-13-pdf-template-engine-design.md) — branding_themes, document_templates_pdf, document_template_versions (immutable, append-only + prevent_document_template_version_mutation trigger); lock-on-issue template_version_id pins on invoices/quotes/case_reports; Report Studio RLS hardening + deleted_at on report_section_library/report_section_presets/master_case_report_templates. Applied to the live DB by the PDF-template-engine work and recorded here (manifest + regenerated types) to restore the schema-discipline gates. | catch-up |
| 20260615061647 | fix_onboarding_progress_user_id | Additive (columns + indexes) | Country Engine Phase 0 D6 — onboarding_progress lacked user_id + deleted_at, so provision-tenant's insert errored and was swallowed (0 rows, dead post-login onboarding wizard). Adds user_id uuid REFERENCES auth.users(id) + deleted_at timestamptz, idx_onboarding_progress_tenant_id (partial) and uq_onboarding_progress_tenant_user (partial unique); provision-tenant now soft-deletes the half-provisioned tenant and throws (fail-loud). database.types.ts regenerated. | country-engine |
| 20260615061727 | add_get_base_currency | Function (currency resolver) | Country Engine Phase 0 D2 — get_base_currency(): SECURITY DEFINER STABLE; tenant_currencies(is_base) → tenants.currency_code → NULL (deliberately NOT USD, to fail loud rather than poison analytics). Foundation for flipping money-column USD defaults in Phase 1. | country-engine |
| 20260615061817 | vat_records_purchase_idempotency | Additive (unique index) | Country Engine Phase 0 D1 — purchase-side input-VAT writer (createVATRecordFromPurchase) so vat_returns.input_vat is real and net VAT filed is correct (was sale-only → overstated liability). Adds uq_vat_records_record unique (record_type, record_id) WHERE deleted_at IS NULL to prevent double-count on edit/re-save. | country-engine |
| 20260615082952 | country_engine_phase1_foundation | Additive (tables + columns + functions + RLS + backfill) | Country Engine Phase 1 foundation (atomic, applied under app.bypass_tenant_guard for the backfill). geo_countries jsonb config_bag + weekend_days/digit_grouping/config_status no-stub guard; new geo_regions, geo_subdivisions, master_data_residency_regions; legal_entities (tenant-scoped RESTRICTIVE; tax identity != tenant); branches promoted to business-unit (legal_entity_id/parent/subdivision/config); nullable scope cols + cases.branch_id FK across cases/invoices/quotes/payments/receipts/stock_sales/custody/case_devices/number_sequences; dormant flag-OFF BU RLS policies + get_current_business_unit_id/get_current_region_id/business_unit_scoping_enabled; sync_tenant_config_from_country ui_language fix (drops USD/$ literal) + _apply_country_config/resync_tenant_country_config + jurisdiction-override governance trigger; master_leave_types/master_payroll_components overlay cols; auto-collapse backfill = 2 tenants -> 1 primary legal_entity + 1 MAIN branch each. | country-engine |
| 20260615100613 | country_engine_phase2_multicurrency_and_notifications | Additive (FX shadow cols + base + overlay) | Country Engine Phase 2 multicurrency gap closure + comms. FX shadow (currency/exchange_rate/rate_source/*_base) added to stock_sales, payroll_records, purchase_orders; receipts.currency_code backfilled from tenant base then NOT NULL; bank_accounts opening/current_balance_base for D8 base cash rollup; notification_templates.template_key + lookup index (overlay, table pre-existed). All additive; currency defaults via get_base_currency() (fail-loud, not USD). | country-engine |
| 20260615134120 | country_engine_phase2_track_a_i18n_tables | Additive (global tables + column) | Country Engine Phase 2 Track A i18n enablers. New global-master geo_languages (seeded en+ar; drives hydrateLanguages — unblocks A1-hydrate) and i18n_translations (DB-backed catalog for the A2 lazy backend + donor ETL); customers_enhanced.preferred_language (Q3 per-recipient comms — unblocks A5-wiring/R9 first rung). Global-master RLS mirrors geo_countries (anon read active, all authenticated read, platform-admin writes). | country-engine |
| 20260615134403 | country_engine_phase2_notification_overlay_and_template_scope | Additive (global table + scope cols + index) | Country Engine Phase 2 R8/R5. master_notification_templates global overlay over tenant-scoped notification_templates (NULL country_id = global default; UNIQUE NULLS NOT DISTINCT on event_type/channel/locale/country_id) — unblocks R10 cascade. document_template_versions.legal_entity_id/business_unit_id + partial-unique uq_template_deployed_scope (NULLs collapse to tenant default) — unblocks R6 per-entity template resolution. | country-engine |
| 20260615142238 | country_engine_phase2_portal_auth_preferred_language | Function (CREATE OR REPLACE, same signature) | Country Engine Phase 2 A5-wiring (Q3). authenticate_portal_customer now returns preferred_language (added to the SELECT INTO + returned json) so the portal resolves the per-recipient UI locale at login; all auth behavior (bcrypt verify, lockout, last-login stamp) preserved verbatim. Signature/return (json) unchanged → no database.types regen. | country-engine |
| 20260615175550 | populate_geo_countries_reference_data | Additive (provenance cols + reference-data seed + is_active flip) | Country Engine Phase 1 geo reference-data seed. Adds geo_countries provenance cols (source_locked/data_source/source_version); upserts the hand-verified GCC-6 + GB/IN/US set (9 countries) with full currency/locale/date/timezone/phone_format/address_format/country_config (cldr+iso4217+libphonenumber, gcc-priority-2026-06-15) via idempotent ON CONFLICT WHERE NOT source_locked; de-activates the other 49 active stubs so no half-configured country stays active (0 active stubs strict). Lets country-config-completeness flip to enforcing. database.types.ts regenerated. | country-engine |
| 20260615200307 | signup_otps_consumed_at | Additive (column) | Single-use signup OTP. Adds signup_otps.consumed_at timestamptz (nullable); provision-tenant now filters .is('consumed_at', null) on the OTP re-verify and atomically claims the row (conditional UPDATE ... WHERE consumed_at IS NULL) so a verified code can't provision twice. Edge-fn code lands in this PR; the prod deploy of provision-tenant is the activation step (owed). database.types.ts regenerated. | country-engine |
| 20260616141836 | set_tenant_country_config_overrides | Functions (2 SECURITY DEFINER RPCs) | Localization Center (Phase 3) write path. set_tenant_country_config_overrides(uuid,jsonb) — ||-merge into tenants.country_config_overrides (no clobber), re-asserts admin authz, audits via log_audit_trail; reset_tenant_country_config_overrides(uuid,text[]) — removes keys with an anti-brick guard (refuses to clear a required key not otherwise resolvable). The validate_country_config_overrides() trigger still rejects statutory keys. EXECUTE granted to authenticated only. database.types.ts regenerated. | country-engine |
| 20260616142738 | backfill_tenant_config_overrides_from_default_locale | Data backfill (idempotent, no-op on current tenants) | Before cutting the accounting_locales resolver fold: lift ONLY the 3 fold keys (currency.code, datetime.date_format, locale.code) from each tenant's default accounting_locales row into country_config_overrides when absent from BOTH resolved_country_config and overrides. isResolvedConfig-preserving; re-run is a no-op. No-op on the 2 live tenants (all 3 keys already in resolved_country_config). | country-engine |
| 20260616150005 | harden_set_config_overrides_reject_required_keys | Function (CREATE OR REPLACE, same signature) | PR-A review hardening (D11): set_tenant_country_config_overrides now rejects required jurisdiction keys (currency.code/locale.code/tax.label/tax.default_rate/number_format.amount_in_words_minor_units) server-side via ?| guard, not only via the client isConfigKeyLocked check — a direct rpc() call can no longer shadow jurisdiction truth. Signature unchanged → no types regen. | country-engine |
| 20260617085112 | 20260617085112_provision_tenant_apply_country_config_on_insert.sql | Trigger (AFTER INSERT) + trigger function | Currency P4 Slice 0 — new tenants were dead-on-arrival: resolved_country_config stayed '{}' because _apply_country_config was only ever called by the resync RPC / P3 backfill, never at INSERT, so getTenantConfig() threw CountryConfigError on required keys (tax.label/tax.default_rate) and the app hard-blocked with "Tenant not configured". Adds AFTER INSERT trigger trg_tenants_apply_country_config (WHEN country_id IS NOT NULL) calling the existing _apply_country_config(NEW.id) — population is now atomic + unbypassable at creation. _apply_country_config builds the bag from geo_countries scalar cols + the country_config jsonb (which carries every required key incl. tax.default_rate — verified OM/US/AE/GB/IN). Verified via a rolled-back test INSERT: a new OM tenant resolves an 18-key bag with all required keys. Reversible (DROP TRIGGER + FUNCTION). No database.types regen (trigger function, not a PostgREST-callable RPC). | currency-localization |
| 20260620020115 | enforce_active_in_get_current_tenant_id | Function (CREATE OR REPLACE, same signature) | M1 (auth lifecycle audit) — get_current_tenant_id() now filters is_active = true in the profiles subquery, matching is_staff_user/is_admin/is_platform_admin which already do. Deactivated users previously kept tenant data access because the RESTRICTIVE tenant-isolation policies resolve tenant via this function. Active users unaffected (primary path unchanged, all 6 live profiles is_active=true); deactivated users resolve NULL and the JWT fallback is empty (no custom access-token hook), so isolation denies every tenant table on deactivation. Signature/return (uuid) unchanged → no database.types regen. | auth-hardening |
| 20260620051740 | case_quotes_portal_read_policy | Additive (RLS policy + grant) | Portal identity read-path (audit C4) — case_quotes was the one portal-reachable table missing a TO portal SELECT policy (it has no direct customer_id; scoped via case_id -> cases.customer_id like case_reports). Adds case_quotes_portal_read + GRANT SELECT ON case_quotes TO portal. Inert for staff/anon (TO portal only); verified by SQL role/claim simulation that a portal principal sees only its own customer's rows. Policy-only → no database.types regen. | portal-identity |
| 20260620053512 | portal_read_policies_and_quote_authz | Additive (5 RLS policies + grants) + 2 function re-scopes (same signature) | Portal identity read-path completion (audit C4) + quote-write authz hardening. Adds customer-scoped TO portal SELECT policies (+ GRANT SELECT TO portal) for case_report_sections (via report->case->customer), case_quote_items (via case_quotes->case->customer), customer_communications (direct customer_id), stock_sales (direct customer_id), master_case_priorities (global lookup, USING true). Re-scopes approve_quote/reject_quote so a portal principal can only act on its own quotes (adds `get_current_portal_customer_id() IS NULL OR customer_id = get_current_portal_customer_id()`; staff path with a null portal claim is byte-identical) and records the portal customer as approver via COALESCE. case_job_history deliberately NOT exposed (raw internal audit log — needs a curated customer timeline). All inert for staff/anon; SQL role/claim simulation verified zero cross-customer leakage on all 5 reads and that approve/reject no-op on another customer's quote. Policies + same-signature function bodies → no database.types regen. | portal-identity |
| 20260620090314 | allow_intra_phase_case_status_transitions | Function (CREATE OR REPLACE, same signature) | Case status-change 400 fix. transition_case_status modelled status changes as phase->phase edges only, so moving between two sub-statuses of the SAME lifecycle phase (intake Registered/Received, diagnosis, qa Verification & QC/Data Transfer, the three Completed and three Cancelled variants) found no edge and RAISEd 23514 -> HTTP 400 — breaking the most basic intake step (Registered -> Received) and every other intra-phase refinement. Adds an intra-phase branch: a same-phase move (same type, different status id) is treated as a staff-gated, audited lateral refinement that skips the edge lookup + evidence gates (qa_passed/recovery/payment, which guard phase boundaries); cross-phase behaviour (edge lookup, role allowlist, release gates) is byte-identical, just moved into the ELSE branch (v_transition stays NULL so details.transition_id is null). Verified via rolled-back impersonated calls: intra-phase Initial Assessment -> Diagnosis in Progress now ok:true + case_job_history written + actor stamped; invalid cross-phase diagnosis -> delivered still blocked (23514); valid diagnosis -> quoting still ok. Paired with a frontend status-picker rebuild (CaseOverviewTab + pure buildCaseStatusOptions helper) that offers only reachable statuses — current + same-phase siblings + role-filtered getAllowedTransitions, grouped into optgroups — replacing the offer-every-status dropdown that produced the 400s. Signature/return (jsonb) unchanged → no database.types regen. | case-workflow |
| 20260620180558 | per_device_checkout_handover | Additive (case_devices columns + partial index + CHECK; idempotent data backfill) + Function (DROP+CREATE, +1 trailing DEFAULT arg) | Per-device checkout handover (Stage 13). Adds case_devices checkout STATE: checked_out_at, checkout_batch_id, checkout_collector_name/_mobile/_id, checkout_collector_relationship (CHECK in self/authorized_agent/company_rep/courier), checkout_by + partial index idx_case_devices_in_lab(case_id) WHERE checked_out_at IS NULL. Best-effort backfill of checked_out_at from chain_of_custody DEVICE_CHECKED_OUT events (2 historical devices). log_case_checkout reworked: +p_collector_relationship (DEFAULT NULL so existing 6-arg named-arg callers resolve unambiguously), stamps the per-device state for p_device_ids, raises check_violation when relationship<>self and National ID is empty, and transitions the case to delivered ONLY when all non-deleted devices are checked out (partial leaves status; legacy null-array path keeps prior best-effort transition). database.types.ts regenerated. | device-checkout |
| 20260621212017 | 20260621212017_expense_lifecycle_schema_hardening.sql | Additive | Expense lifecycle hardening (audit 2026-06-21). Lifecycle cols on expenses (submitted_by/submitted_at, rejection_reason/rejected_by/rejected_at, updated_by, paid_at — all nullable); status DEFAULT changed 'approved'->'draft' so raw/CSV inserts no longer auto-approve & bypass the ledger (EXP-013/EXP-031); status CHECK (draft/pending/approved/rejected/paid/voided); partial UNIQUE index uq_financial_transactions_expense_ref on financial_transactions(reference_type,reference_id) WHERE reference_type='expense' AND deleted_at IS NULL — DB-enforced idempotency that makes the EXP-001 double-post impossible; 5 partial perf indexes on expenses (status/case_id/category_id/expense_date/created_by). The single live row is 'pending' so all changes are non-breaking. database.types.ts regenerated. | expense-hardening |
| 20260621231646 | 20260621231646_create_expense_receipts_storage_bucket.sql | Additive (storage) | EXP-052/053 — create the PRIVATE expense-receipts bucket (it never existed, so receipt uploads always failed) + 4 tenant-folder RLS policies on storage.objects (SELECT/INSERT/UPDATE/DELETE gated on (storage.foldername(name))[1] = get_current_tenant_id()::text, writes also is_staff_user), modeled on case-report-pdfs. Upload path tenant-prefixed in uploadExpenseAttachment so a flat path can't be cross-tenant readable. No public-schema change → no database.types regen. | expense-hardening |
| 20260622074117 | 20260622074117_expense_disbursement_atomic_rpc.sql | Additive (column) + Function | EXP-017 — make approved→paid real. Adds payment_disbursements.expense_id (FK→expenses, partial index) for "which account paid this, when" + future reversal. New SECURITY DEFINER RPC record_expense_disbursement(p_expense_id, p_bank_account_id, p_paid_at DEFAULT CURRENT_DATE, p_reference) that, atomically: authorizes via has_role('accounts'), locks the expense (FOR UPDATE), guards status='approved' (this is the idempotency — a 2nd call sees 'paid' and errors), validates the account (tenant, active, currency MATCHES the expense — match-currency v1), BLOCKS on insufficient balance, then inserts payment_disbursements (status 'completed') + bank_transactions (type 'debit', running_balance) and debits bank_accounts.current_balance(_base), and stamps the expense paid/paid_at/bank_account_id. Accrual GL already posted at approval (EXP-024) so NO second financial_transactions post. REVOKE from anon; GRANT to authenticated. database.types.ts regenerated (expense_id column + function signature). | expense-disbursement |
| 20260622084315 | 20260622084315_expenses_compute_base_amounts_trigger.sql | Additive (trigger function) | EXP-062 — imports/raw inserts left expenses.amount_base/tax_amount_base NULL (currency defaulted 'USD', rate 1), silently corrupting cross-currency report totals via the amount_base??amount*rate fallback. Adds SECURITY DEFINER fn set_expenses_base_amounts() + BEFORE INSERT/UPDATE trigger zz_set_expenses_base_amounts (zz_ sorts after set_expenses_tenant_and_audit so NEW.tenant_id is set) that fills amount_base/tax_amount_base = round(amount*rate, base.decimals) ONLY when NULL — no-op for createExpense/updateExpense which already pass them. One-time additive backfill of NULL-base non-deleted rows. Trigger fn (returns trigger) not PostgREST-exposed → no database.types regen. Paired frontend: ENTITY_CONFIGS.expenses gains currency/tax_amount/exchange_rate/is_billable importable fields + ImportWizard maps them. | expense-backlog |
| 20260622090552 | 20260622090552_enforce_expense_status_transition_trigger.sql | Additive (trigger function) | EXP-009 — DB-side state-machine backstop. RLS expenses_update=has_role('accounts') has no status predicate, so a raw table UPDATE could bypass the service guards (draft→paid, approved→rejected leaving a dangling ledger row). BEFORE UPDATE trigger zz_enforce_expense_status_transition validates OLD.status→NEW.status against the shipped edge set (draft/rejected→pending, pending→approved, pending→rejected, approved→paid; →voided only via soft-delete; voided terminal), RAISE check_violation otherwise; WHEN clause fires only on status change/soft-delete so field edits are zero-overhead. Mirrors the service guards exactly so nothing breaks; record_expense_disbursement's approved→paid still allowed. Trigger-only → no database.types regen. Rollback in file header. | expense-backlog |
| 20260622091022 | 20260622091022_expense_archive_authority_and_reconciliation.sql | Additive (2 functions + trigger) | EXP-018 + EXP-006. (a) BEFORE UPDATE trigger trg_enforce_expense_archive_authority gates the deleted_at NULL→non-NULL transition (the real delete-equivalent) to admin (honors app.bypass_tenant_guard for backfills), converging the cosmetic UI gate + dead expenses_delete policy. (b) archive_expense(p_expense_id,p_reason) SECURITY DEFINER RPC = the single soft-delete path: admin-gated, FOR UPDATE, idempotent; for an approved expense reverses the GL accrual via reverse_financial_transaction (append-only, now()-dated — no back-dating) + retires purchase/expense vat_records, cascades attachment soft-delete, sets deleted_at+status='voided'. A PAID expense is BLOCKED (must un-pay the disbursement first — avoids bank/GL desync; the original void design that direct-credited balances was rejected in review). deleteExpense→archiveExpense alias; handleBulkArchive loops it (was a raw bulk UPDATE that orphaned the ledger). (c) reconcile_expense_ledger(date,date) read-only diagnostic — per-period expenses-vs-GL delta, nets reversals. database.types regenerated (archive_expense + reconcile_expense_ledger). Rollback in file header. | expense-backlog |
| 20260622091558 | 20260622091558_assert_expense_import_allowed.sql | Additive (function) | EXP-063 — wizard-scoped server preflight for expense bulk import: assert_expense_import_allowed(p_row_count) SECURITY DEFINER, authorizes has_role('accounts'), enforces the bulk_import feature + max_expenses_per_month quota for the caller's tenant (mirrors client resolution: plan_features row wins, else hierarchy [bulk_import≥professional], else NULL=unlimited; counts current-month non-deleted expenses). ImportWizard calls it once before the insert loop and aborts on denial. NOT insert-path enforcement — expenses_insert RLS unchanged, a direct API insert still bypasses; live DB has 0 plan_features rows so the gate is effectively open until operators seed billing config. database.types regenerated (assert_expense_import_allowed). Paired EXP-064 (no migration — actor stamping already covered by EXP-010's trigger): ImportWizard per-row insert fallback so one bad FK no longer sinks a 100-row batch + surfaces failures; pre-insert reference validation DEFERRED. Rollback in file header. | expense-backlog |
| 20260622092246 | 20260622092246_exp053_expenses_select_staff_only.sql | RLS policy (tighten) | EXP-053 — expenses_select was USING(true): every authenticated tenant principal incl. read-only viewer read all expense financials. Now USING(is_staff_user() OR is_platform_admin()) — excludes viewer (+ inactive/soft-deleted profiles); RESTRICTIVE expenses_tenant_isolation still ANDs tenant scoping. Submitter-scoping NOT applied (would undercount unscoped report/case-profit aggregations for technician/sales/hr — EXP-011's submitter-scope deferred for this reason). Policy-only → no database.types regen. Verified live. Rollback in file header. | expense-backlog |
| 20260622092335 | 20260622092335_exp054_tighten_financial_transactions_rls.sql | RLS policy (tighten) | EXP-054 — financial_transactions_select was USING(true) and _insert WITH CHECK is_staff_user(). Now _select USING(is_staff_user()) (viewer loses ledger read) and _insert WITH CHECK(has_role('accounts')). All money RPCs are SECURITY DEFINER (bypass RLS) so unaffected; only direct-insert paths (expense approval, CSV revenue/transactions import) are accounts-gated. Behavior change: technician/sales/hr lose direct CSV import of revenue/transactions (intended). RESTRICTIVE tenant isolation untouched. Policy-only → no database.types regen. Verified live. Rollback in file header. | expense-backlog |
| 20260622090631 | 20260622090631_expense_actor_stamping_trigger.sql | Additive (trigger function) | EXP-010 (security core) — actor fields were client-spoofable (created_by from client, approved_by/rejected_by from a passed profile id). BEFORE INSERT/UPDATE trigger zset_expenses_actor_fields FORCES created_by/updated_by/submitted_by/approved_by/rejected_by from auth.uid() server-side and pins created_by to OLD on UPDATE (immutable authorship). Writes ONLY actor columns — never amount/currency/status/tenant_id/updated_at — so the EXP-019 optimistic lock and FX snapshots are unaffected. DEFERRED: the per-action audit_trails EMISSION half (log_audit_trail 6/8-arg overload 400s via PostgREST named-params — needs its own resolution). Trigger-only → no database.types regen. Rollback in file header. | expense-backlog |
| 20260625183522 | 20260625183522_consolidate_device_catalogs.sql | Data (DML consolidation) | Tier 2 catalog consolidation — seed 23 granular interfaces into catalog_interfaces + deactivate 8 coarse parents (USB/SATA/M.2/NVMe/PCIe/FireWire/SD-MMC/Ethernet); retire catalog_device_interfaces (is_active=false, 0 FK refs); add Donor Drives/Head Assemblies/Motors to master_inventory_categories; retire master_inventory_item_categories; deprecation comments on both retired tables + inventory_items.item_category_id. All affected FK columns had 0 rows (verified 2026-06-25). No DDL → database.types.ts unchanged. | device-catalog |
| 20260625215132 | 20260625215132_add_case_devices_technical_fields.sql | Additive (DDL) | Device-details dynamic form — adds case_devices.dom (date), part_number (text), dcm (text), technical_details (jsonb NOT NULL DEFAULT '{}'). Additive nullable/defaulted; existing rows untouched; RLS/triggers unaffected (new cols inherit case_devices policies); all 4 targets verified empty pre-apply (zero data-migration risk). database.types.ts regenerated (+16 lines). | device-details |
| 20260626210759 | 20260626210759_create_case_device_activity.sql | Additive (DDL) | New tenant-scoped table case_device_activity — per-device discrete activity log (component_status_updated / diagnostic_note_added / diagnostic_test_performed / component_note_added / device_received) powering the Edit Device → History/Activity tab. Full tenant-table compliance: tenant_id NOT NULL FK, RLS enabled+forced, RESTRICTIVE tenant isolation, set_case_device_activity_tenant_and_audit trigger, idx_case_device_activity_tenant_id + (device_id, created_at DESC) partial indexes. Written client-side from the device-save diff; forensic chain_of_custody untouched. database.types.ts regenerated (+79 lines). Rollback in file header. | device-history |
| 20260627172503 | document_studio_enums | Additive (enums) | Document Studio Phase 2 — 4 enums (document_instance_type, document_instance_status, signature_method, signature_slot) for the unified document lifecycle. Spec docs/superpowers/specs/2026-06-27-document-studio-design.md. database.types.ts regenerated. | document-studio |
| 20260627172705 | create_document_instances | Additive (table + RLS + triggers + guard) | Universal generated-document record replacing the case_reports.content JSONB lifecycle blob: typed status/version/approval/delivery/sign-off + resolved_data snapshot + pdf_sha256 provability; second-person CHECK (approved_by<>created_by); RESTRICTIVE tenant isolation; authoring-only UPDATE policy + enforce_document_instance_lifecycle guard trigger (app.bypass_document_guard) so approval/delivery/artifact columns move only via RPC. database.types.ts regenerated. | document-studio |
| 20260627172735 | create_document_instance_sections | Additive (table + RLS) | Per-report editable prose body (migration target of case_report_sections); the editor overlay on binding-resolved defaults. Full tenant-table compliance. database.types.ts regenerated. | document-studio |
| 20260627172832 | create_document_signatures | Additive (table + RLS + append-only) | Captured signatures (typed/drawn/uploaded/click_to_accept) per signer slot; append-only after signing via prevent_audit_mutation; unique (instance, slot); deferred FK document_instances.customer_signoff_signature_id. database.types.ts regenerated. | document-studio |
| 20260627173027 | create_document_status_transitions | Additive (table + seed) | Global table-driven lifecycle edges (mirrors case_status_transitions): doc_type/from/to/allowed_roles[]/requires[]; seeded with draft->in_review->approved->delivered + reject/void edges. database.types.ts regenerated. | document-studio |
| 20260627173235 | document_lifecycle_rpcs | Additive (3 functions + portal policy/grants) | SECURITY DEFINER lifecycle: transition_document_instance (role-gated, second-person, send-gate requires pdf+sha256, qa_passed evidence gate; writes audit_trails + chain_of_custody), set_document_instance_artifact (provability write: pdf path/sha256 + resolved_data snapshot), portal_sign_off_document (portal-scoped via get_current_portal_customer_id; inserts customer signature + flips signed_off + custody). + document_instances_portal_read policy + GRANTs to portal. database.types.ts regenerated. | document-studio |
| 20260627195955 | rename_report_sections_friendlier | Data (label update) | Document Studio Phase 7 — friendlier canonical report section names in report_section_library (system rows): findings→"Diagnostic Findings", recommendations→"Proposed Solution", destruction_certificate→"Certificate of Destruction". The library is the single source the editor/Studio read; the PDF resolves titles by section_key via the DOCUMENT_TRANSLATIONS i18n system (13 languages). Data-only, reversible; no types regen. | document-studio |
| 20260627180701 | case_diagnostics_recoverability | Additive (2 columns + CHECKs) | Document Studio Phase 5 — adds case_diagnostics.recoverability_pct (0-100 CHECK) + recoverability_assessment (fully/partially/unrecoverable/requires_donor/pending) — the evaluation report's engineer-set recoverability indicator. Additive nullable, table empty (0 rows) → zero data risk; activates case_diagnostics writes via the new diagnosis capture UI. database.types.ts regenerated. | document-studio |
| 20260629130539 | portal_read_own_delivered_document_pdfs | Additive (storage RLS + grant) | Document Studio Phase 9 — lets the portal role SELECT its OWN delivered/signed-off document PDFs (bucket case-report-pdfs) so a customer can review before sign-off. GRANT SELECT ON storage.objects TO portal + one fail-closed policy scoped via get_current_portal_customer_id (di.pdf_storage_path = objects.name, delivered/visible, owns case). No other bucket reachable; no types regen (storage policy). | document-studio |
| 20260629164703 | migrate_legacy_reports_to_instances | Data (additive, idempotent) | Document Studio Phase 11 — one-time migration of historical legacy reports into document_instances so they stay viewable after the legacy stack is retired. Adds document_instances.legacy_case_report_id (provenance + idempotency key, partial unique index) + copies each non-deleted case_reports row -> document_instances (doc_type=report, status mapped, number disambiguated on collision) and case_report_sections -> document_instance_sections. case_reports/sections are the FROZEN source-of-record, never modified/dropped. Bypasses tenant/document guards (no JWT in migration). database.types.ts regenerated. | document-studio |
| 20260629202115 | drop_legacy_report_tables | Destructive (DROP) | Phase 11 follow-up (owner decision) — DROP the 6 retired legacy report tables (case_reports, case_report_sections, master_case_report_templates, report_section_library, report_section_presets, report_template_section_mappings) + the dead increment_preset_usage() fn. Code + history already migrated to document_instances in PR #340; applied post-merge after the new code was confirmed live. Supersedes the freeze runbook. document_instances.legacy_case_report_id kept as origin metadata. database.types.ts regenerated; eslint bans retained. | document-studio |
| 20260629215312 | device_types_master_inventory_attributes | Additive (global catalog) | Inventory V2 P0 — augment catalog_device_types (Master Device Type): + family, icon, default_category_id, inventory_prefix, inventory_padding, barcode_prefix, qr_prefix, is_inventory_tracked; add PCB + Head Stack device types; backfill family/prefix/icon/category for all 20 types (per-type prefixes BIG/SMALL/SSD/M2/NVME/...). Global table (no tenant_id); per-tenant default location deferred to P5. database.types.ts regenerated. | inventory-v2 |
| 20260629220437 | inventory_items_device_type_and_specs | Additive (tenant) | Inventory V2 P1 — inventory_items + device_type_id (FK catalog_device_types), technical_details jsonb, barcode, qr_value; GIN index on technical_details + device_type index + unique (tenant_id,barcode) + qr_value index. Table empty so no backfill. database.types.ts regenerated. | inventory-v2 |
| 20260629220723 | get_next_inventory_number_per_device_type | Additive (RPC) | Inventory V2 P2 — get_next_inventory_number(device_type_id) SECURITY DEFINER: lazy-seeds a per-tenant number_sequences row from the device type catalog prefix/padding then calls atomic get_next_number(scope=inventory:<id>). Per-device-type independent sequences (BIG/SMALL/SSD/...); replaces the client-side race. EXECUTE granted to authenticated only. database.types.ts regenerated. | inventory-v2 |
| 20260629222734 | create_inventory_donor_parts | Additive (tenant table) | Inventory V2 P4 — inventory_donor_parts (id, tenant_id, item_id, part_type, quantity, condition_id, notes, audit/soft-delete). Device-type-driven donor parts replacing the hardcoded 3-key donor_parts_available JSONB. RLS forced + RESTRICTIVE isolation + staff/admin permissive policies + set_*_tenant_and_audit/audit_actor triggers + idx_*_tenant_id partial + item/part_type indexes + unique (item_id,part_type). database.types.ts regenerated. | inventory-v2 |
| 20260629224636 | create_tenant_device_type_settings | Additive (tenant table) | Inventory V2 P5 — tenant_device_type_settings (per-tenant per-device-type config; default_location_id FK inventory_locations). Holds tenant overrides the global catalog_device_types cannot (default storage location). Full RLS forced + RESTRICTIVE isolation + staff-read/manager-write/admin-delete + triggers + idx_*_tenant_id partial + unique (tenant_id,device_type_id). database.types.ts regenerated. | inventory-v2 |
| 20260629230000 | search_donor_drives_technical_details_match | RPC update (no signature change) | Inventory V2 P9 — extend search_donor_drives to filter technical_details (pcb_number/firmware_version/dcm/physical_head_map/controller/chipset) + device_type_id + serial, rank exact PCB/firmware/DCM matches first. Function body only; signature unchanged so no type regen. | inventory-v2 |
| 20260629231500 | deprecate_legacy_inventory_item_columns | Deprecation (COMMENT only) | Inventory V2 P10 — mark legacy inventory_items columns DEPRECATED (pcb_number/firmware_version/head_map → technical_details; donor_parts_available → inventory_donor_parts). Columns now unused (readers repointed). No-DROP discipline: kept + reversible; hard DROP is a separate owner call. Comments only → no type change. | inventory-v2 |
| 20260630070526 | inventory_item_number_assigned_by_trigger | Fix (trigger + data cleanup) | Inventory V2 — BEFORE INSERT trigger trg_assign_inventory_item_number assigns item_number/barcode/qr_value via get_next_inventory_number(device_type_id) inside the insert txn (fixes the allocate-before-insert leak where a failed insert burned a sequence number with no item). Reclaims phantom inventory:* sequence values while inventory is empty. Client no longer pre-allocates. | inventory-v2 |
| 20260630101808 | device_service_catalogs_writable_by_admins | RLS (policy change) | Owner decision — lab Admins/Owners can manage the device/media + case/service reference catalogs from Settings. Switched INSERT/UPDATE/DELETE policies on 19 global catalog/master tables (catalog_device_*, catalog_service_*, master_inventory_*, master_case_*, catalog_device_conditions/roles) from is_platform_admin() to is_admin() (owner/admin any scope). SELECT untouched; tables remain shared/global. | inventory-v2 |
| 20260630120000 | 20260630120000_data_migration_schema.sql | Additive (2 tenant tables) | Unified import/export engine P0 — data_migration_runs (run ledger: kind/status checks, totals/counts/error_summary jsonb, file_hash) + data_migration_entity_map (legacy_id->new_id remap + idempotency backbone, unique (run_id,entity_type,legacy_id)). Both: tenant_id NOT NULL FK, RLS enabled+forced, RESTRICTIVE tenant isolation, staff read/write + admin delete policies, set_*_tenant_and_audit + audit_actor triggers, idx_*_tenant_id partial. Plus uq_data_migration_runs_active_import partial unique (tenant_id,file_hash) WHERE kind='import' AND status<>'completed' for resumable imports. database.types.ts regenerated. | import-export |
| 20260630120100 | data_migration_trigger_guards | Function (3 trigger bodies) | Import/export P0 — prepend an app.importing guard to the three fabricating after-insert trigger functions (log_device_received_custody/custody, post_invoice_vat_record/VAT, seed_portal_customer_subscriptions/portal subs); all existing logic preserved verbatim. Import RPC sets app.importing transaction-local so migrated rows do not manufacture import-dated custody/VAT/portal events. Body-only → database.types.ts unchanged. | import-export |
| 20260630120200 | data_migration_drop_legacy | Destructive (DROP) | Import/export P0 — clean-slate retirement: DROP the 4 legacy import_export_* tables (templates/jobs/logs + import_field_mappings, all 0-row) and the 8 import-only lookup_* resolver functions. git grep verified zero non-import callers (sole caller importExportService.ts removed in the same phase). database.types.ts regenerated (those tables/functions removed). | import-export |
| 20260630195733 | data_migration_export_rpc | Additive (RPC) | Import/Export P2 — data_migration_export_page(p_entity_type, p_after_created_at, p_after_id, p_limit, p_filters) SECURITY DEFINER, search_path=public. Tenant-scoped keyset pagination on (created_at,id); returns {rows,next} shaped to the workbook contract (row id->legacy_id, parent uuids->*_legacy_id, catalog uuids->names via catalog_device_types/brands/capacities/catalog_interfaces/conditions). EXECUTE granted to authenticated only. database.types.ts regenerated. | import-export |
| 20260630201824 | 20260630201824_data_migration_import_rpcs.sql | Additive (RPCs) | Import engine P3 — data_migration_create_run (resume-aware: returns existing non-completed import run for same tenant+file_hash else inserts — completed-run resume added later by 20260630225429), data_migration_import_batch (per-row SAVEPOINT loop; idempotent against data_migration_entity_map; remaps parent *_legacy_id via data_migration__resolve helper; SET LOCAL app.importing/app.bypass_tenant_guard; inserts into real target tables with tenant_id + preserved created_at + preserved numbers + metadata.legacy_id on customers; records legacy_id->new_id in the map in the same savepoint), data_migration_finalize (advances number_sequences.current_value past max imported suffix for case/customers/companies/quote/invoices scopes (invoices scope matches get_next_invoice_number → get_next_number('invoices')); writes one audit_trails IMPORT_FINALIZED row + one MIGRATED case_job_history note per imported case; marks run completed). All SECURITY DEFINER. database.types.ts regenerated. | import-export |
| 20260630222509 | data_migration_import_dedup | Function (RPC body) | Import engine C2 — data_migration_import_batch now does DB-level duplicate detection BEFORE insert (tenant-scoped, deleted_at IS NULL): customers by metadata.legacy_id/email/phone, companies by name/email, cases by case_number, invoices by invoice_number, quotes by quote_number. Duplicate -> status='skipped_duplicate' (reason recorded), NOT inserted, but entity_map still maps legacy_id->existing_uuid so children resolve to the existing parent. Keyless children (relationships/devices/quoteItems/invoiceLineItems/notes/statusHistory) have no business key and no metadata column -> rely on per-run entity_map idempotency. Makes a completed-run re-upload insert 0 rows (C3). Body-only -> database.types.ts unchanged. | import-export |
| 20260630225429 | data_migration_resume_completed_and_idempotent_finalize | Function (2 RPC bodies) | Import engine C3 fix — re-uploading the SAME completed workbook duplicated keyless children (devices/quoteItems/invoiceLineItems/notes/statusHistory): create_run resumed only status<>'completed' runs, so a completed re-upload minted a NEW run + fresh entity_map; keyed parents deduped via C2 but keyless children (no business key, no metadata col) plain-INSERTed dup uuids (duplicating append-only case_job_history → forensic-audit violation). Fix: (1) data_migration_create_run now RESUMES the most-recent (tenant,file_hash,kind='import') run REGARDLESS of status — a completed run is returned AS-IS (status untouched, so uq_data_migration_runs_active_import partial index is not tripped and the finalized run is preserved), an in-flight run keeps prior resume behavior; the persisted entity_map then short-circuits EVERY entity (keyed+keyless) → 0 inserts on re-upload. (2) data_migration_finalize is now IDEMPOTENT — a completed run no-ops (returns {already_finalized:true}; does NOT re-advance number_sequences, write a 2nd IMPORT_FINALIZED provenance row, or add a 2nd MIGRATED note). import_batch unchanged. Verified via rolled-back live round-trip (project ssmbegiyjivrcwgcqutu): RUN2 create_run returned RUN1's id; devices/quoteItems/invoiceLineItems/notes/statusHistory all 0 new rows; finalize2 already_finalized; exactly 1 provenance row. Body-only → database.types.ts unchanged. | import-export |
| 20260701135704 | add_companies_metadata_column | Additive (tenant column) | Import/export round-trip B2 — companies + metadata jsonb NOT NULL DEFAULT '{}' so the data_migration engine can stamp provenance (legacy_id, data_migration_run_id) and dedupe companies by legacy_id, at parity with customers_enhanced. Engine-internal, not exposed in the workbook. Additive, nullable-defaulted (no backfill). database.types.ts regenerated. | import-export |
| 20260701135818 | data_migration_export_companies_full_fields | Function (RPC body) | Import/export B1 — data_migration_export_page companies branch now emits contact_person/contact_email/contact_phone/is_active (defined in the workbook contract but dropped on export → round-trip data loss). Only the companies branch changed; every other branch reproduced verbatim. Body-only → database.types.ts unchanged. | import-export |
| 20260701140001 | data_migration_import_companies_full_fields_and_metadata | Function (RPC body) | Import/export B1/B2/B4 — data_migration_import_batch: companies INSERT persists the 4 contact/is_active fields + metadata.legacy_id/run_id; companies dedup gains a legacy_id-first branch (parity with customers); relationships + cases assert both resolved parents (customer/company) belong to the run's tenant (defense-in-depth cross-tenant guard). Every other branch verbatim. Body-only → database.types.ts unchanged. | import-export |
| 20260701140435 | add_tenant_code_column_and_immutability | Additive (column + trigger) | Tenant-code A1 — tenants + tenant_code text (human-readable, platform-unique, immutable support id; UUID stays the sole PK/FK/isolation key). Partial unique index on live rows, format CHECK ^[A-Z]{2,4}[0-9]{4,}$, and BEFORE UPDATE trigger enforce_tenant_code_immutable (hard-blocks value→value changes; admits NULL→value only via assign_tenant_code's app.assigning_tenant_code GUC — RLS alone is insufficient because tenants_update lets a tenant admin write their own row). database.types.ts regenerated. | tenant-code |
| 20260701140623 | create_platform_tenant_code_prefixes | Additive (platform table) | Tenant-code A2 — platform_tenant_code_prefixes (platform-admin-curated geography→prefix map; country_id XOR region_id, prefix ^[A-Z]{2,4}$). RLS enabled+forced with a permissive is_platform_admin() policy. Seeded UAE→MED, Oman→OMA, India→IND (code3 is null for these countries, so the registry is the only meaningful prefix source). | tenant-code |
| 20260701140652 | create_platform_tenant_code_sequences_and_assign_fn | Additive (platform table + RPC + backfill) | Tenant-code A3/A5 — platform_tenant_code_sequences (per-prefix cross-tenant counter; the per-tenant number_sequences cannot be used) + assign_tenant_code(uuid) SECURITY DEFINER (idempotent; prefix resolves curated country→region→code3→code||X; race-safe ON CONFLICT increment; format PREFIX+LPAD(N,4) e.g. OMA0001; sets app.assigning_tenant_code to pass the immutability trigger). Backfill assigns codes to existing tenants ordered by created_at (2 Oman tenants → OMA0001/OMA0002). database.types.ts regenerated. | tenant-code |
| 20260701141434 | harden_tenant_code_function_grants | Security (grants) | Tenant-code hardening — Supabase default privileges grant EXECUTE on new public functions directly to anon/authenticated, so REVOKE FROM public alone left them REST-callable. REVOKE EXECUTE on assign_tenant_code(uuid) from PUBLIC/anon/authenticated + GRANT to service_role only (mutating fn, edge-client only); REVOKE EXECUTE on the enforce_tenant_code_immutable trigger fn from PUBLIC/anon/authenticated (never a REST RPC; the trigger still fires — runtime trigger invocation ignores EXECUTE privilege). | tenant-code |
| 20260701145647 | data_migration_export_devices_full_fields | Function (RPC body) | Device round-trip fidelity (export). Root cause: the devices branch emitted key 'serial' (mismatch → buildWorkbook read contract key 'serial_number' → undefined → serial LOST at export) and omitted the forensic columns. Now emits serial_number + part_number/firmware_version/pcb_number/dcm (donor-matching), dom, physical_damage/symptoms/diagnosis, recovery_result/data_recovered_size, storage_location, notes, is_primary — all keys matching the contract. Only the devices branch changed; every other branch verbatim. Body-only → database.types.ts unchanged. | import-export |
| 20260701145814 | data_migration_import_devices_full_fields | Function (RPC body) | Device round-trip fidelity (import). The devices INSERT persisted only model/serial_number/symptoms/notes and dropped is_primary (the "patient" device) + the forensic columns. Now inserts all contract device columns (serial_number, part_number, firmware_version, pcb_number, dcm, dom::date, physical_damage, symptoms, diagnosis, recovery_result, data_recovered_size, storage_location, notes, is_primary). Only the devices branch changed; every other branch verbatim. Verified via rolled-back live import (serial + pcb/fw/dcm/part + dom + is_primary + fault/outcome all persisted). Body-only → database.types.ts unchanged. | import-export |
| 20260701152137 | data_migration_finalize_autofill_blank_numbers | Function (RPC body) | Import number policy (finalize v1, superseded by 20260701152949) — after advancing each number_sequences scope past the max SUPPLIED number, backfill rows whose case/customer/company/quote/invoice number is blank by minting strictly above (reusing the tenant's prefix/padding). Collision-safe because it runs after all rows are in. Body-only. | import-export |
| 20260701152300 | data_migration_import_preserve_company_number | Function (RPC body) | Import number policy — the companies INSERT now persists company_number (was dropped → imported companies always got NULL, unlike customers). Preserves file-supplied numbers; blanks handled by finalize. Superseded by 20260701152832 (same body + invoice placeholder). Body-only. | import-export |
| 20260701152832 | data_migration_import_invoice_number_placeholder | Function (RPC body) | Import number policy — invoices has CHECK (invoice_number IS NOT NULL), so a blank invoice number can't be inserted as null for finalize to fill. Insert the row's own UUID as a placeholder (non-null, unique, unmistakable); finalize replaces it with a minted number. Carries the company_number preserve fix. Only the invoices INSERT changed vs 20260701152300; all else verbatim. Body-only. | import-export |
| 20260701152949 | data_migration_finalize_autofill_with_placeholder | Function (RPC body) | Import number policy (finalize final) — extends the blank backfill to also fill the invoices UUID-placeholder rows, and EXCLUDES placeholder rows from the max-supplied computation (their UUID digits would otherwise inflate the sequence). Verified via rolled-back live import: supplied numbers preserved (CUST-5000/COMP-8000/CASE-3000/INV-7000), blanks auto-filled strictly above (INVO-7001 > INV-7000), company_number imported, invoice placeholder replaced (not a UUID), no collisions. Body-only. | import-export |
| 20260701163047 | data_migration_export_inventory | Function (RPC body) | Inventory import/export — export branches for inventoryLocations, inventoryItems, inventoryDonorParts. Catalog device_type/brand/capacity/interface names via LEFT JOINs to the GLOBAL catalogs; condition/status/category via the INVENTORY master tables (master_inventory_condition_types/status_types/categories); location_id/parent_id/item_id emitted as raw uuids (entity-map refs); technical_details emitted as a jsonb object (client builder JSON-stringifies it). Only 3 new branches; all else verbatim. Body-only. | inventory |
| 20260701163330 | data_migration_import_inventory | Function (RPC body) | Inventory import/export — import branches. Locations (soft self-parent → NULL when unresolved); items (catalog by name globally, condition/status/category by name against inventory masters, category default from device type, location via entity-map ref with location_code/name fallback, technical_details handles JSON-string and object, item_number preserved-if-supplied with barcode/qr reconstructed else NULL so the BEFORE INSERT trigger assigns a per-device-type number); donor parts (item entity-map ref, condition by name). New DECLARE locals; all existing branches + C2 dedup verbatim. Body-only. Verified rolled-back live: hierarchy resolved, preserve + auto-assign (SMALL-0001) + null-number paths, masters/location resolved, technical_details queryable, donor parts linked. | inventory |
| 20260701163423 | data_migration_finalize_inventory_sequences | Function (RPC body) | Inventory import/export — finalize gains a per-device-type block: for each device type the run touched, seed the 'inventory:<device_type_id>' number_sequences row (prefix/padding from catalog_device_types, mirroring get_next_inventory_number) and GREATEST-advance it past the max imported item_number suffix, so preserved numbers can't collide with future app-created items. Idempotency short-circuit preserved. Body-only. | inventory |
| 20260701203517 | data_migration_import_full_fields_and_catalog_resolution | Function (RPC body) | Import completeness — data_migration_import_batch was inserting only a minimal column subset per entity, silently dropping most contract fields. Rewrote every record-domain INSERT to persist ALL contract fields: cases (priority + resolved priority_id, diagnosis, resolution, recovery_outcome, referred_by, client_reference, is_urgent, is_warranty, estimated/actual_completion), companies (tax_number, registration_number), customers (whatsapp_number, company_name, id_type, id_number, tax_number, source, is_active), quotes/invoices (title, type, currency, exchange_rate, discount, tax_rate, dates, terms, client_reference, amounts), quote/invoice line items (discount, tax_rate). Devices: catalog resolution now exact→normalized(strip spaces/quotes so "2.5' HDD"→"2.5\" HDD", "1 TB"→"1TB")→alias(SDXC/SDHC→SD Card, Flash Drive→USB Drive, Micro SD→MicroSD Card), plus device_role_id defaulted to Patient (id 1) so the Devices tab classifies them. Superseded by the two fix-forwards below. Body-only. | import-export |
| 20260701204003 | data_migration_import_fix_generated_title_column | Function (RPC body) | Fix-forward: cases.title / case_no / assigned_engineer_id and companies.company_name are GENERATED columns; removed cases.title from the INSERT (subject drives generated title). Body-only. | import-export |
| 20260701204333 | data_migration_import_coalesce_discount_type_notnull | Function (RPC body) | Fix-forward (FINAL, authoritative body): quotes.discount_type / invoices.discount_type are NOT NULL (default 'fixed'); COALESCE the file value to 'fixed'. Verified via rolled-back all-entity live smoke: all 11 record entities insert; device "2.5' HDD"→2.5" HDD, "1 TB"→1TB, "Micro SD"→MicroSD Card, "128 GB"→128GB, role=Patient; case priority/priority_id/is_urgent/is_warranty/diagnosis persisted. Body-only. | import-export |
| 20260701223211 | data_migration_import_financial_entities_and_gl | Function (RPC body) | Financial import — added import branches for bankAccounts (balances set directly), payments (invoice/bank/customer/case via entity-map + payment_method by name against GLOBAL master_payment_methods), receipts (customer money-in), expenses (category by name against GLOBAL master_expense_categories + bank/case; status lowercased to satisfy the CHECK). Each payment/receipt posts a financial_transactions GL row (type 'income') and each expense a GL row (type 'expense', status 'posted') — the prevent_financial_transactions_mutation guard fires only on UPDATE/DELETE so INSERT is allowed. New DECLARE locals v_bank/v_pay_method/v_exp_cat/v_currency_id; all prior branches verbatim. Verified rolled-back live: bank/payment/receipt/expense insert, expense "Paid"→paid, 3 GL rows (income 500 + income 200 + expense 80). Body-only. | import-export |
| 20260701223523 | data_migration_export_financial_entities | Function (RPC body) | Financial export — export_page branches for bankAccounts/payments/receipts/expenses (payment_method + expense category emitted as NAMES via LEFT JOIN to the global masters); also enriched the cases/quotes/invoices export branches to emit the full contract field set (diagnosis/resolution/priority/etc.) for a true round-trip. Body-only. | import-export |
| 20260701224852 | emit_payment_received_event_skip_during_import | Function (trigger) | Recheck fix — emit_payment_received_event (AFTER INSERT on payments) wrote 1-2 notification_events rows per payment with NO app.importing guard, so a bulk historical-payment import would flood staff/customer "payment.received" notifications. Added `IF current_setting('app.importing',true)='true' THEN RETURN NEW` at the top (mirrors the 3 other fabricating triggers). Verified: import payment → 0 notification_events, GL still posts. Body-only. | import-export |
| 20260701225038 | data_migration_reference_lists | Function (new RPC) | Reference sheet — SECURITY DEFINER STABLE fn data_migration_reference_lists(p_domain) returns jsonb of {label: [names]} for the domain's name-resolved global masters (records: device types/brands/capacities/interfaces/conditions, case priorities, payment methods, expense categories, currencies, invoice+expense statuses; inventory: catalogs + inventory masters). GRANT EXECUTE authenticated. Client buildWorkbook/buildTemplateWorkbook append a "Reference (Valid Values)" sheet (parser ignores it). database.types.ts regenerated (+4 lines: the new fn). | import-export |
| 20260701230839 | data_migration_import_payment_aliasing_and_allocations | Function (RPC body) | Follow-ups (both). (1) payment_method resolution now exact → normalized (regexp_replace [^a-z0-9]) → curated alias map (cash/cod→Cash, card/cc/visa→Credit Card, bank/neft/imps/rtgs→Bank Transfer, cheque→Check, wire→Wire Transfer, ach→ACH Transfer, upi/gpay/applepay/wallet→Mobile Payment, crypto/btc→Cryptocurrency, po→Purchase Order, dd→Direct Debit, etc.); expense category now exact → normalized. (2) payment_allocations row inserted per invoice-linked payment (payment_id/invoice_id/amount) for AR reconciliation — payment_allocations has NO trigger that recomputes invoices.amount_paid (only set_payment_allocations_tenant_and_audit), so it coexists with the imported amount_paid WITHOUT double-counting. New DECLARE v_pm/v_ec. Verified rolled-back live: cheque→Check, Bank→Bank Transfer, GPay→Mobile Payment, 3 allocations totalling 300. Body-only. | import-export |
| 20260701234900 | data_migration_import_tier1_tier2_entities | Function (RPC body) | Tier1+2 import — 26 new branches (customerGroups→customers group wiring, accountTransfers [NO balance side-effect], paymentDisbursements, creditNotes/items/allocations, customer/case communications, caseRecoveryAttempts, deviceDiagnostics [jsonb result], cloneDrives→resource_clone_drives, suppliers [category/terms by name], supplierContacts, purchaseOrders [status by name], purchaseOrderItems [stock item by sku live], stockCategories/Locations/Items [writes quantity_on_hand/reorder_level since current/minimum/available are GENERATED; opening qty posts an 'adjustment' stock_transactions row]/SerialNumbers/Sales [customer/case live by business key]/SaleItems, departments, positions, employees [soft manager self-ref], leaveBalances [leave type by name], employeeLoans) + dedup guards (group/supplier/po/cn/sku/sale/dept/position/employee). Superseded by the fix below. Body-only. | import-export |
| 20260701235511 | data_migration_import_t12_fix_notnull_dates | Function (RPC body, FINAL) | Fix-forward: credit_notes.credit_note_date + account_transfers.transfer_date are NOT NULL DEFAULT now() — explicit NULL bypasses the default → COALESCE to created_at/now(). Verified rolled-back: 34/34 entity inserts incl. group membership, opening stock txn, manager link, computed leave remaining, cross-domain sale links. Body-only. | import-export |
| 20260701235634 | add_credit_note_allocations_updated_at | Schema (additive) | Pre-existing bug: credit_note_allocations lacks updated_at but its set_*_tenant_and_audit trigger assigns NEW.updated_at → EVERY insert failed ('record new has no field updated_at'). Added updated_at timestamptz NOT NULL DEFAULT now(). | schema-fix |
| 20260701235953 | data_migration_export_tier1_tier2_entities | Function (RPC body) | Tier1+2 export — 26 new branches (names via LEFT JOINs: supplier category/terms, PO status, stock category/location/supplier number, payment methods, leave types, clone-drive catalogs; customers now emit customer_group; stockSales emit customer_ref=email/number/name + case_number). Verified: 44/44 entity exports OK. Body-only. | import-export |
| 20260702000029 | add_deleted_at_stock_sale_items_leave_balances | Schema (additive) | Platform-convention gap: stock_sale_items + leave_balances lacked deleted_at (soft-delete filter) → export branches errored. Added deleted_at timestamptz NULL to both. | schema-fix |
| 20260702000109 | data_migration_reference_lists_new_domains | Function (RPC body) | Reference sheet — per-domain lists for procurement (supplier categories, payment terms, PO statuses, currencies), stock (payment methods, currencies, payment statuses), hr (leave types, currencies); records gains credit-note status/type literals. Body-only. | import-export |
| 20260702003108 | seed_catalog_capacities_and_brands_realworld | Seed (global catalogs, additive) | Real-world gaps from the rev2 lab-migration audit: +11 capacities (4.7GB DVD, 30GB, 146GB/300GB/600GB enterprise SAS, 240GB/960GB/3.84TB SSD, 1.2TB/2.4TB SAS, 15TB) with gb_value, then ALL capacities re-sequenced by parsed size (KB/MB/GB/TB → sort_order) so pickers stay ordered; +37 brands (Lexar, Team Group, HGST, Sony, Hikvision, HIKSEMI, Asus, Acer, Imation, Huawei, PNY, LG, KIOXIA, SK Hynix, Verbatim, TwinMOS, Microsoft, OnePlus, Netgear, Apacer, Xiaomi, Iomega, IBM, Panasonic, MSI, Lite-On, Garmin, Realme, JVC, Kodak, Oppo, Honor, Patriot, XPG, KingSpec, Freecom, Hyundai). WHERE NOT EXISTS guards (idempotent). Global tables → benefits all current & future tenants. Totals now: 66 capacities, 62 brands. | catalog-seed |
| 20260702011736 | data_migration_import_denormalized_customer_case_links | Function (RPC body, FINAL) | Broken-relations fix — quotes + invoices carry their OWN customer_id/company_id (list pages read them directly, showed "N/A"): both branches now derive customer/company from the resolved case and INSERT them; payments (and credit notes) derive missing case_id/customer_id from the linked invoice. Verified rolled-back: invoice.customer/quote.customer/payment.case+customer all set, 0 errors. Data backfill for the already-imported tenant ran separately (993 invoices + 1138 quotes customer/company from case; 1 payment case from invoice). Body-only. | import-export |
| 20260702012501 | bank_accounts_normalize_type_and_base_on_insert | Function (trigger) | Banking import hygiene — BEFORE INSERT trigger on bank_accounts: normalizes free-text account_type into the app vocabulary (checking/savings→kept, credit card/other unknowns→bank, cash→cash, mobile/wallet→mobile) so getAccountBalanceSummary buckets imported accounts correctly, and fills current_balance_base/available_balance_base = value × COALESCE(exchange_rate,1) when NULL so base-currency tiles are never 0.000 for imported accounts. Applied during the SPACE DATAA RECOVERY banking-drift fix; benefits all future tenant imports. | banking |
| 20260702031404 | case_status_counts_and_flow_rpcs | Function (2 new RPCs) | Cases command-center data layer — get_case_status_counts() (per-status counts, powers truthful KPI buckets + data-driven status chips) and get_case_flow_stats(from,to) (received/delivered counts + median TAT days via percentile_cont on checkout_date−created_at). Both SECURITY INVOKER STABLE — RLS scopes to the caller's tenant. GRANT EXECUTE authenticated. database.types.ts regenerated. | cases-ux |
| 20260702031418 | seed_space_recovery_case_status_type_overrides | Data (tenant metadata seed) | Lifecycle truth fix for tenant SPACE DATAA RECOVERY — 13 imported legacy status names mapped to master_case_statuses.type vocabulary in company_settings.metadata.case_status_types (Returned→delivered, Waiting for Approval→awaiting_approval, Under Inspection→diagnosis, Unrecoverable/MOVED TO 3K SERIES 2026→cancelled, etc.). Global master vocabulary deliberately NOT polluted with tenant-specific strings; resolution layers overrides over master rows client-side (caseLifecycle.ts). | cases-ux |
| 20260702032511 | seed_space_recovery_cases_table_new_defaults | Data (tenant metadata seed) | Cases-list P1 rollout for SPACE DATAA RECOVERY — saved column config predates the new age/engineer columns (saved visible wins over registry defaults), so tenant defaults reset to the operator-first layout (case_no, age, customer, status, device_type, device_model, serial_primary, capacity, engineer; priority/contact/client-ref out — phone stacks under customer) and the one stale per-user cases override dropped (preferences #- '{tables,cases}'). | cases-ux |
| 20260702054458 | update_number_sequence_settable_current_value | Function (RPC, replaced signature) | Sequence editors could change prefix/padding but never the counter — a drifted/freshly-seeded sequence was unfixable from the UI (owner hit this on inventory 2.5" HDD: modal-created row seeded current_value=1 with no control). DROP 4-arg + recreate with p_current_value integer DEFAULT NULL: NULL leaves the counter untouched (existing SystemNumbers/inventory callers unchanged), a value sets it (UI passes next−1, guard >= 0); INSERT path now seeds 0 (next = -0001) instead of burning the first number. Verified via rolled-back live smoke (set→107, null-call untouched). database.types.ts regenerated. | inventory |
