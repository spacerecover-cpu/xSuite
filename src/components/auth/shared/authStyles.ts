// Shared class strings for the auth zone's primary controls (lint-exempt
// non-themed surface — see DESIGN.md). One definition so the login, forgot,
// and reset CTAs stay identical.

export const AUTH_PRIMARY_BUTTON =
  'w-full py-3.5 bg-gradient-to-r from-sky-500 to-violet-600 hover:from-sky-400 hover:to-violet-500 text-white font-medium text-sm rounded-xl shadow-lg shadow-violet-950/50 transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white/70 focus-visible:ring-offset-2 focus-visible:ring-offset-slate-900 disabled:opacity-60 disabled:cursor-not-allowed flex items-center justify-center gap-2';
